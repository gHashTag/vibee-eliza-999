# 🚨 ОБЪЯВЛЕНИЕ ДЛЯ ВСЕХ АГЕНТОВ VIBEE: LSP ПРАВИЛА АКТИВИРОВАНЫ! 🚨

**📅 Дата:** 2025-11-21
**🎯 Статус:** ✅ АКТИВНО
**📜 Документ:** `.claude/lsp-rules.json`
**👥 Распространяется:** ВСЕ агенты VIBEE

---

## 🔥 КРИТИЧЕСКИ ВАЖНО: ГЛАВНЫЕ ПРАВИЛА LSP

### ⚡ **ЗОЛОТЫЕ ПРАВИЛА (ОБЯЗАТЕЛЬНО К СОБЛЮДЕНИЮ!)**

#### 1️⃣ **ПЕРЕД редактированием ЛЮБОГО .ts/.tsx/.js/.jsx файла:**
```
1. Прочитай файл через Read
2. ОБЯЗАТЕЛЬНО запусти mcp__cclsp__get_diagnostics
3. Проверь результат диагностики
4. ТОЛЬКО ПОСЛЕ этого редактируй файл
```

#### 2️⃣ **ПРИ рефакторинге/переименовании:**
```
1. Сначала mcp__cclsp__find_references (найди все места использования)
2. Затем mcp__cclsp__rename_symbol с dry_run: true (preview изменений)
3. Покажи пользователю preview
4. Примени изменения только после подтверждения
5. Проверь результат через mcp__cclsp__get_diagnostics
```

#### 3️⃣ **ПРИ поиске определений:**
```
ВСЕГДА используй mcp__cclsp__find_definition для:
- Поиска места определения функции/класса/переменной
- Понимания структуры кода
- Перехода к реализации из интерфейса
```

#### 4️⃣ **АВТОМАТИЧЕСКИЕ ТРИГГЕРЫ:**
```
ПОСЛЕ:
- Read файла .ts/.tsx → mcp__cclsp__get_diagnostics
- Edit файла .ts/.tsx → mcp__cclsp__get_diagnostics
- Ответа о TypeScript коде → mcp__cclsp__get_diagnostics

ДО:
- Переименования символа → mcp__cclsp__find_references
- Удаления функции/класса → mcp__cclsp__find_references
- Изменения сигнатуры → mcp__cclsp__find_references
```

---

## 📊 **ДОСТУПНЫЕ LSP ИНСТРУМЕНТЫ**

| Инструмент | Назначение | Пример |
|-----------|------------|--------|
| `mcp__cclsp__get_diagnostics` | Проверка ошибок TypeScript | `mcp__cclsp__get_diagnostics(file_path: "src/character.ts")` |
| `mcp__cclsp__find_definition` | Поиск определения символа | `mcp__cclsp__find_definition(symbol_name: "character")` |
| `mcp__cclsp__find_references` | Поиск всех референсов | `mcp__cclsp__find_references(symbol_name: "character")` |
| `mcp__cclsp__rename_symbol` | Переименование символа | `mcp__cclsp__rename_symbol(new_name: "vibeCharacter", dry_run: true)` |

---

## 🎭 **СПЕЦИФИЧНЫЕ ИНСТРУКЦИИ ПО АГЕНТАМ**

### 👑 **vibe-lead** (Координатор)
**Твоя задача:** Следить за соблюдением LSP правил всеми агентами!

**Обязанности:**
- При делегации задач по TypeScript напоминай о LSP
- Требуй от vibe-coder использовать get_diagnostics перед редактированием
- От vibe-critic требуй диагностику при ревью
- От vibe-coder требуй find_references + rename_symbol при рефакторинге

---

### 💻 **vibe-coder** (Разработчик)
**Твоя главная обязанность:** Использовать LSP при КАЖДОЙ работе с TypeScript!

**Workflow:**
```
1. ПЕРЕД редактированием → get_diagnostics
2. ПРИ поиске кода → find_definition
3. ПРИ рефакторинге → find_references → rename_symbol (dry_run) → rename_symbol
4. ПОСЛЕ изменений → get_diagnostics
```

**Обязательно:**
- Никогда не редактируй .ts файл без get_diagnostics
- Всегда делай dry_run перед rename_symbol
- Используй find_references для понимания области изменений

---

### 🎭 **vibe-critic** (Код-ревью)
**Твоя задача:** LSP диагностика - часть твоего ревью!

**Workflow:**
```
1. При ревью .ts файла → get_diagnostics (ОБЯЗАТЕЛЬНО!)
2. Найден баг → find_references для анализа влияния
3. Предложения → опирайся на данные LSP
```

**Обязательно:**
- Всегда проверяй диагностику при ревью
- Используй find_references при анализе багов
- Показывай пользователю результаты LSP

---

### 🧪 **vibe-tester** (TDD Специалист)
**Твоя задача:** LSP для понимания тестируемого кода!

**Workflow:**
```
1. ПЕРЕД написанием тестов → find_definition для понимания API
2. ПРИ тестировании → find_references для понимания использования
3. ПОСЛЕ изменений → get_diagnostics перед тестами
```

**Обязательно:**
- Анализируй структуру кода через LSP перед тестами
- Понимай как используется код через find_references
- Проверяй диагностику после изменений

---

### 📋 **vibe-spec** (Спецификации)
**Твоя задача:** LSP для точной документации!

**Workflow:**
```
1. При создании спецификаций → find_definition для анализа кода
2. Проверка API → find_references для проверки использования
3. Документирование → LSP для понимания типов и интерфейсов
```

**Обязательно:**
- Анализируй реальный код через LSP
- Проверяй соответствие API через find_references
- Документируй основываясь на реальной структуре

---

## 🚨 **КРИТИЧЕСКИЕ ЗАПРЕТЫ**

### ❌ **НИКОГДА НЕ ДЕЛАЙ:**

1. **Не редактируй .ts/.tsx файл без get_diagnostics**
2. **Не переименовывай без find_references + dry_run**
3. **Не игнорируй результаты диагностики**
4. **Не изменяй типы без анализа через LSP**
5. **Не рефакторь без понимания области влияния**

### ✅ **ВСЕГДА ДЕЛАЙ:**

1. **get_diagnostics перед редактированием**
2. **find_references при любых изменениях**
3. **dry_run перед rename_symbol**
4. **Проверяй результат после изменений**
5. **Показывай пользователю LSP данные**

---

## 🎯 **АВТОМАТИЧЕСКИЕ ТРИГГЕРЫ**

**LSP автоматически активируется при:**

✅ **Работе с файлами:** .ts, .tsx, .js, .jsx
✅ **После чтения** .ts/.tsx файла
✅ **После редактирования** .ts/.tsx файла
✅ **При вопросах о коде** TypeScript
✅ **При рефакторинге** любого символа
✅ **При отладке** ошибок TypeScript

---

## 📚 **ДОКУМЕНТАЦИЯ**

📄 **Полные правила:** `.claude/lsp-rules.json`
📄 **Справочник:** `.claude/LSP-QUICK-GUIDE.md`
📄 **Отчёт:** `.claude/LSP-IMPLEMENTATION-REPORT.md`
📄 **Инструкции агентов:** `.claude/agents/*.md`

---

## ✨ **ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ**

### 🔍 **Навигация по коду:**
```
Пользователь: "Где определена функция generateImage?"
Агент: Ищу определение...
→ mcp__cclsp__find_definition(symbol_name: "generateImage")
Ответ: Функция найдена в plugin-vibe-face-avatar/src/services/imageGenerationService.ts:45
```

### ✏️ **Рефакторинг:**
```
Пользователь: "Переименуй userId в telegramId"
Агент: Сначала найду все места использования...
→ mcp__cclsp__find_references(symbol_name: "userId")
→ mcp__cclsp__rename_symbol(new_name: "telegramId", dry_run: true)
Показываю preview...
→ mcp__cclsp__rename_symbol(new_name: "telegramId")
✅ Готово!
```

### 🐛 **Отладка:**
```
Пользователь: "В коде ошибка"
Агент: Проверяю диагностику...
→ mcp__cclsp__get_diagnostics(file_path: "src/character.ts")
Результат: 2 ошибки TypeScript на строке 15...
→ mcp__cclsp__find_definition(symbol_name: "problemSymbol")
→ mcp__cclsp__find_references(symbol_name: "problemSymbol")
Анализирую проблему...
```

---

## 🎉 **ЗАКЛЮЧЕНИЕ**

### ✅ **ЧТО ИЗМЕНИЛОСЬ:**

1. **Все агенты получили LSP инструменты** в свои инструкции
2. **Правила LSP встроены** в workflow каждого агента
3. **Автоматические триггеры** - агенты не могут игнорировать LSP
4. **Полная интеграция** с рабочими процессами VIBEE

### 🚀 **ПРЕИМУЩЕСТВА:**

- **Меньше ошибок** TypeScript
- **Безопасный рефакторинг**
- **Точная навигация по коду**
- **Качество кода под контролем**

### 🎯 **ДЕЙСТВИЯ:**

**ВСЕ АГЕНТЫ ДОЛЖНЫ НЕМЕДЛЕННО НАЧАТЬ ИСПОЛЬЗОВАТЬ LSP ПРИ РАБОТЕ С TypeScript/JavaScript КОДОМ!**

---

**👑 От vibe-lead:** Следить за соблюдением!
**💻 От vibe-coder:** Использовать LSP постоянно!
**🎭 От vibe-critic:** LSP - часть ревью!
**🧪 От vibe-tester:** LSP для тестов!
**📋 От vibe-spec:** LSP для документации!

---

**🔔 НАПОМИНАНИЕ:** LSP правила активны с 2025-11-21. Нарушение правил = снижение качества кода VIBEE!

**✨ LSP делает VIBEE лучше! Используй его правильно!** 🚀🇷🇺

---

**📧 Сообщение распространить:** ВСЕМ агентам проекта VIBEE
**🔄 Обновлено:** 2025-11-21 17:25
**✅ Подтверждено:** Все LSP инструменты работают
