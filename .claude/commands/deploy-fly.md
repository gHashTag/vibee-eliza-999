# fly-deploy - Принудительный деплой на Fly.io

## Описание
Принудительно развертывает приложение на Fly.io с очисткой всех кэшей.
Критически важно для исправления ошибок кэширования, таких как:
- `ERR_UNSUPPORTED_DIR_IMPORT` с telegram
- Устаревшие версии кода в продакшене

## Когда использовать
1. **Fly.io показывает старую ошибку** - даже после исправлений в коде
2. **Нужно обновить production** - принудительно обновить до последней версии
3. **Кэш build сломался** - принудительно пересобрать все с нуля

## Синтаксис
```bash
/deploy-fly
```

## Что происходит
1. ✅ Проверяет конфигурацию fly.toml
2. ✅ Запускает `fly deploy --no-cache --detach`
3. ✅ Использует Depot builder для чистой сборки
4. ✅ Передает весь код без кэширования
5. ✅ Отслеживает статус сборки

## Примеры

### Принудительный деплой
```
/deploy-fly
```

## Информация
- **Файл команды**: `.claude/commands/deploy-fly.md`
- **Время выполнения**: 3-5 минут
- **Платформа**: Fly.io
- **Регион**: ams (Amsterdam)
- **Приложение**: vibee-eliza-999-prod

## Решение проблем
Если деплой не помогает:
1. Проверьте логи: `fly logs --app vibee-eliza-999-prod`
2. Убедитесь что код собирается локально: `bun run build`
3. Проверьте что все зависимости установлены: `bun install`

---
🤖 Команда создана автоматически для проекта vibee-eliza-999
