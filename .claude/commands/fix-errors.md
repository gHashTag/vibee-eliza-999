# fix-errors - Исправление типичных ошибок

## Описание
Автоматически исправляет самые частые ошибки проекта vibee-eliza-999.

## Синтаксис
```bash
/fix-errors
```

## Что исправляет
1. ❌ **"Cannot find package 'uuid'"**
   - Проверяет node_modules
   - Пересобирает plugin-sql: `bun run build --filter @elizaos/plugin-sql`
   - Проверяет external dependencies в build.ts

2. ❌ **"ERR_UNSUPPORTED_DIR_IMPORT" (telegram)**
   - Исправляет импорты в plugin-telegram-craft
   - Добавляет .js расширения: `telegram/sessions/index.js`
   - Пересобирает и коммитит

3. ❌ **PGLite errors**
   - Проверяет migration файлы
   - Добавляет try-catch для schema operations
   - Очищает старую БД

4. ❌ **TypeScript сборка**
   - Очищает .turbo, dist, *.tsbuildinfo
   - Пересобирает все: `bun run build`
   - Проверяет tsconfig.json

5. ❌ **Client vite errors**
   - Устанавливает vite@5.4.11
   - Исправляет package.json dependencies

## Когда использовать
- Любая ошибка сборки
- Ошибка запуска
- Плагин не работает
- Fly.io не деплоится

## Последовательность
1. Полная очистка кэшей
2. Переустановка зависимостей
3. Пересборка всех пакетов
4. Проверка TypeScript
5. Коммит изменений
6. Деплой на Fly.io

## Результат
✅ Все пакеты собраны без ошибок
✅ Локальный запуск работает
✅ Production готов к деплою

---
🤖 Автоматическое исправление для проекта vibee-eliza-999
