# quick-start - Быстрый старт разработки

## Описание
Автоматическая настройка и запуск проекта vibee-eliza-999 в режиме разработки.

## Синтаксис
```bash
/quick-start
```

## Что происходит
1. ✅ Очищает все процессы (Ctrl+C для всех elizaos/bun)
2. ✅ Проверяет занятость портов 3000 и 5173
3. ✅ Очищает кэш TypeScript (.turbo, dist, *.tsbuildinfo)
4. ✅ Пересобирает все пакеты (`bun run build`)
5. ✅ Запускает в режиме разработки (`bun run dev`)

## Порт заняты?
Если порт занят, выполните:
```bash
lsof -ti:3000 | xargs kill -9
lsof -ti:5173 | xargs kill -9
```

## Ожидаемый результат
```
[DEV] Server running at http://localhost:3000
[DEV] Client running at http://localhost:5173
```

## Важные команды
- **Остановка**: Ctrl+C
- **Логи**: Проверяйте в реальном времени
- **Пересборка**: При изменениях кода автоматически

## Решение проблем
- ❌ "Cannot find module 'uuid'": Пересоберите `bun run build`
- ❌ Порт занят: Завершите процессы на портах 3000/5173
- ❌ TypeScript ошибки: `bun run build` и исправьте ошибки

---
🤖 Автоматическая команда для проекта vibee-eliza-999
