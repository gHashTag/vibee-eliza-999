name: Manual Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Test step
        run: |
          echo "✅ Manual test worked!"
          echo "Time: $(date)"
          echo "Event: ${{ github.event_name }}"
