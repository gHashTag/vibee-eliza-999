# 🤖 Агенты VIBEE - Инструкция по использованию

## Проблема "Loading agents..."

Агенты не подгружаются автоматически - это нормально для ElizaOS! Агенты нужно запускать вручную.

## ✅ Решение

### 1. Экспорт агентов в JSON

```bash
node scripts/export-agents.js
```

Создаст файлы в папке `agents-json/`:
- `vibee.json` - главный агент
- `neurophoto.json` - генерация изображений
- `instagram-expert.json` - Instagram автоматизация
- `kols-agent.json` - мониторинг групп

### 2. Запуск агента

```bash
# Запустить главного агента VIBEE
elizaos agent start --path agents-json/vibee.json

# Запустить агента Нейрофото
elizaos agent start --path agents-json/neurophoto.json

# Посмотреть список запущенных агентов
elizaos agent list

# Остановить агента
elizaos agent stop --name VIBEE
```

### 3. Веб-интерфейс

После запуска агента откройте:
```
http://localhost:3000
```

Там вы увидите агентов во вкладке "Agents"

## 📁 Структура агентов

### VIBEE
- **Роль**: Главный AI-агент проекта
- **Функции**: Управление агентами, плагины, Радужный Мост
- **Плагины**: SQL, Bootstrap

### Нейрофото
- **Роль**: Генерация изображений через ИИ
- **Функции**: LoRA обучение, генерация картинок
- **Модели**: Fal.ai, Replicate

### Instagram Expert
- **Роль**: Работа с Instagram
- **Функции**: Публикация постов, хэштеги
- **Интеграция**: Instagram API

### KOLS Agent
- **Роль**: Мониторинг групп
- **Функции**: Отслеживание активности, статистика
- **Каналы**: Telegram группы

## 🔧 Устранение неполадок

### "Loading agents..." в веб-интерфейсе
- Агенты загружаются через API
- Убедитесь, что агент запущен: `elizaos agent list`
- Проверьте логи: `elizaos agent logs --name VIBEE`

### Ошибка "Cannot find module"
- Убедитесь, что собран проект: `bun run build`
- Проверьте путь к JSON файлу агента

### Агент не отвечает
- Проверьте переменные окружения в `.env`
- Убедитесь, что подключены API ключи (OpenAI, Anthropic)

## 🚀 Автозагрузка агентов

Чтобы агенты запускались автоматически при старте системы, создайте скрипт:

```bash
#!/bin/bash
# start-agents.sh

echo "🚀 Запуск агентов VIBEE..."

# Запускаем всех агентов
elizaos agent start --path agents-json/vibee.json --port 3000 &
elizaos agent start --path agents-json/neurophoto.json --port 3001 &
elizaos agent start --path agents-json/instagram-expert.json --port 3002 &
elizaos agent start --path agents-json/kols-agent.json --port 3003 &

echo "✅ Все агенты запущены!"
```

## 📚 Дополнительная информация

- Документация ElizaOS: https://elizaos.github.io/eliza/
- CLI команды: `elizaos --help`
- Примеры агентов: `packages/agents/`

---

**Примечание**: В будущем планируется добавить автозагрузку агентов при старте сервера.
