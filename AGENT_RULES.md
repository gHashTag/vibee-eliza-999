# ⚠️ КРИТИЧЕСКИЕ ПРАВИЛА ДЛЯ AI АГЕНТОВ ⚠️

## 🔴 ЗАПРЕЩЕНО ПОД УГРОЗОЙ УДАЛЕНИЯ:

### 1️⃣ НИКОГДА НЕ УДАЛЯТЬ АГЕНТОВ
- ❌ **НЕ УДАЛЯЙ** файлы в `characters/*.json`
- ❌ **НЕ УДАЛЯЙ** персонажей, которых ты не создавал
- ❌ **НЕ УДАЛЯЙ** работающих агентов
- ❌ **НЕ ИЗМЕНЯЙ** конфигурацию агентов без явного указания

**Текущие агенты (НЕ ТРОГАТЬ!):**
- `characters/vibeeAgent.json` - Главный агент VIBEE
- `characters/instagramExpert.json` - Instagram эксперт
- `characters/kolsAgent.json` - KOLS мониторинг
- `characters/neuroPhoto.json` - NeuroPhoto генерация

### 2️⃣ ОСНОВНОЙ АГЕНТ VIBEE

```bash
# ✅ ПРАВИЛЬНО - запускает основного агента VIBEE
bun dev

# ✅ ПРАВИЛЬНО - явно запускает основного агента
bun start

# ✅ ПРАВИЛЬНО - запускает всех агентов (список)
bun dev:all

# ⚠️ ТОЛЬКО ПО ПРОСЬБЕ - запускает конкретного агента
bun dev:single characters/vibeeAgent.json
```

**ВАЖНО:** ElizaOS не поддерживает запуск нескольких агентов одновременно!

### 3️⃣ АВАТАРЫ АГЕНТОВ

Аватары хранятся в:
- `/Users/playra/vibee-agent/avatars/` - основная директория
- `/Users/playra/vibee-agent/vibee-client/public/avatars/` - для веб-клиента

**Правила:**
- ✅ Генерировать аватары для НОВЫХ агентов
- ❌ НЕ УДАЛЯТЬ существующие аватары
- ✅ Использовать золотистую цветовую схему (#ffd700 и варианты)

## 🟢 ОБЯЗАТЕЛЬНЫЕ ДЕЙСТВИЯ:

### При создании нового агента:
1. Создать файл `characters/новый-агент.json`
2. Сгенерировать аватар через DiceBear
3. Сохранить аватар в `avatars/` и `vibee-client/public/avatars/`
4. Добавить путь к аватару в `settings.avatar`

### При запуске проекта:
```bash
# Всегда использовать эту команду
cd /Users/playra/vibee-agent
bun dev  # Запустит ВСЕХ агентов
```

### При проверке агентов:
```bash
# Проверить список агентов
ls -la characters/*.json

# Проверить процессы
ps aux | grep elizaos

# Проверить логи
tail -f agent.log
```

## 📋 Структура проекта (НЕ МЕНЯТЬ!):

```
vibee-agent/
├── characters/               # ❌ НЕ УДАЛЯТЬ файлы отсюда!
│   ├── vibeeAgent.json      # Главный агент
│   ├── instagramExpert.json # Instagram
│   └── kolsAgent.json       # KOLS
├── avatars/                 # ❌ НЕ УДАЛЯТЬ аватары!
├── scripts/
│   └── dev-with-infisical.sh # Скрипт запуска
└── package.json             # bun dev = запуск ВСЕХ
```

## 🆘 Если что-то пошло не так:

### Агент пропал?
1. Проверь `ls -la characters/*.json`
2. Проверь git: `git status`
3. Восстанови из git: `git checkout characters/`

### Запускается не тот агент?
1. Проверь `ps aux | grep elizaos`
2. Останови: `pkill -f elizaos`
3. Запусти правильно: `bun dev`

### Нет логов?
1. Скрипт использует `exec npx elizaos start`
2. Логи идут прямо в терминал
3. Проверь `tail -f agent.log`

## 💡 Частые ошибки:

❌ **ОШИБКА:** Запустил `bun dev characters/vibeeAgent.json`
✅ **ПРАВИЛЬНО:** `bun dev` (запустит всех)

❌ **ОШИБКА:** Удалил `kolsAgent.json` потому что не понял зачем он
✅ **ПРАВИЛЬНО:** Оставил все агенты как есть

❌ **ОШИБКА:** Изменил `package.json` и `bun dev` запускает одного
✅ **ПРАВИЛЬНО:** `bun dev` ВСЕГДА должен запускать `--all`

---

# 🔥 ЗАПОМНИ НА ЛБУ:

1. **`bun dev` = ВСЕ АГЕНТЫ**
2. **НЕ УДАЛЯТЬ `characters/*.json`**
3. **НЕ УДАЛЯТЬ аватары**
4. **ТОЛЬКО ДОБАВЛЯТЬ, НЕ УДАЛЯТЬ**

---

_Эти правила написаны кровью и слезами после многократных удалений агентов._
_Не повторяй ошибок прошлого. Уважай чужой код._