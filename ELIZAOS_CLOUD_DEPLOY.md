# ☁️ ДЕПЛОЙ ОФИЦИАЛЬНОГО ELIZAOS В ОБЛАКЕ

## ✅ ЛОКАЛЬНО ПРОТЕСТИРОВАНО

Официальный ElizaOS успешно запущен локально на порту 3002 с полным React UI!

## 🚀 ВАРИАНТЫ ОБЛАЧНОГО ДЕПЛОЯ

### ВАРИАНТ 1: Railway (РЕКОМЕНДУЕТСЯ)

#### Шаг 1: Создать аккаунт на Railway
```bash
1. Перейти на https://railway.app
2. Зарегистрироваться через GitHub
3. Авторизоваться
```

#### Шаг 2: Создать новый проект
```bash
1. Нажать "New Project"
2. Выбрать "Deploy from GitHub repo"
3. В списке найти: vibee-eliza-999
4. Выбрать ветку: develop
5. Нажать "Deploy Now"
```

#### Шаг 3: Настроить переменные окружения
В Railway Dashboard → Variables добавить:

```env
# 🔐 ОБЯЗАТЕЛЬНО: AI API ключи (минимум один)
OPENAI_API_KEY=your_openai_key_here

# 📊 База данных (опционально - Railway предложит PostgreSQL)
# Если не указывать, будет использоваться PGLite (локальная БД)
POSTGRES_URL=postgresql://user:pass@host:5432/db

# 🔧 Конфигурация приложения
SERVER_PORT=3000
NODE_ENV=production

# 🧠 Infisical Cloud (опционально - для управления секретами)
INFISICAL_CLIENT_ID=88fcf0cd-cce9-4844-bad2-8e19b4bad3ed
INFISICAL_CLIENT_SECRET=b377e7a60b669ea2317f339dc6cb79ce49d588a7bbed92433bb2a73dedff3314
INFISICAL_PROJECT_ID=fd763fa3-35d5-4045-93bd-1795c5f00fc3
INFISICAL_ENVIRONMENT=dev
```

#### Шаг 4: Дождаться деплоя
Railway автоматически:
- ✅ Установит зависимости (`bun install`)
- ✅ Соберет проект (`bun run build`)
- ✅ Запустит сервер (`bun run start`)
- ✅ Выдаст URL типа: `https://your-app-production-xxx.up.railway.app`

#### Шаг 5: Проверить деплой
После успешного деплоя:
1. **Web Interface:** `https://xxx.up.railway.app`
2. **API Status:** `https://xxx.up.railway.app/api/status`
3. **API Agents:** `https://xxx.up.railway.app/api/agents`

---

### ВАРИАНТ 2: Собственный сервер (188.137.250.69)

#### Предварительные требования:
- SSH доступ к серверу
- Docker установлен
- Порты 4000 и 5000 свободны

#### Скрипт деплоя:
```bash
#!/bin/bash
# Деплой официального ElizaOS на облачный сервер

# 1. Остановить старые контейнеры
docker stop elizaos-official 2>/dev/null || true
docker rm elizaos-official 2>/dev/null || true

# 2. Создать рабочую директорию
mkdir -p /opt/elizaos-official
cd /opt/elizaos-official

# 3. Установить bun если не установлен
if ! command -v bun &> /dev/null; then
    curl -fsSL https://bun.sh/install | bash
    export PATH="$HOME/.bun/bin:$PATH"
fi

# 4. Скачать код (если нет Git доступа)
# Вариант А: Git clone
git clone https://github.com/elizaOS/eliza.git .
# Вариант Б: Загрузка архива
# wget https://codeload.github.com/elizaOS/eliza/zip/refs/heads/main -O eliza.zip
# unzip eliza.zip && mv eliza-main/* .

# 5. Установить зависимости
export SKIP_POSTINSTALL=1
bun install --no-cache

# 6. Собрать проект
bun run build

# 7. Запустить на портах 4000/5000
export NODE_ENV=production
export PORT=4000
export VITE_PORT=5000

nohup bun run start > /opt/elizaos-official.log 2>&1 &

echo "✅ Официальный ElizaOS запущен!"
echo "📍 URLs:"
echo "   Backend API:  http://188.137.250.69:4000"
echo "   Frontend UI:  http://188.137.250.69:5000"
```

---

### ВАРИАНТ 3: DockerHub + Cloud Server

#### Сборка образа:
```bash
# На локальной машине
cd /tmp/eliza-official
docker build -t your-dockerhub/elizaos-official:latest .

# Загрузить в DockerHub
docker login
docker push your-dockerhub/elizaos-official:latest
```

#### Деплой на сервере:
```bash
docker pull your-dockerhub/elizaos-official:latest
docker run -d \
  --name elizaos-official \
  -p 4000:4000 \
  -p 5000:5000 \
  your-dockerhub/elizaos-official:latest
```

---

## 💰 Стоимость

### Railway Pricing:
- **Hobby Plan:** $0/месяц (бесплатный тариф)
- **Pro Plan:** $20/месяц (продакшн готов)
- **Usage:** Pay-as-you-go за ресурсы

**Рекомендация:** Hobby Plan для тестирования, Pro для продакшена.

---

## 🎯 РЕЗУЛЬТАТ

После деплоя у вас будет:
- ✅ Публичный URL
- ✅ Официальный React UI ElizaOS (НЕ Grafana!)
- ✅ Backend API
- ✅ Автоматические деплойменты при git push
- ✅ Логи и мониторинг
- ✅ SSL сертификат (HTTPS)
- ✅ Масштабирование при нагрузке

---

## 📋 TODO Список

- [ ] Выбрать вариант деплоя (Railway рекомендуется)
- [ ] Создать аккаунт на Railway (если нет)
- [ ] Подключить GitHub репозиторий
- [ ] Настроить переменные окружения
- [ ] Запустить деплой
- [ ] Проверить URLs

---

## 🚨 ВАЖНО

**ОФИЦИАЛЬНЫЙ ELIZAOS UI ОТЛИЧАЕТСЯ ОТ GRAFANA!**

При правильном деплое вы увидите:
- ✅ React-based интерфейс
- ✅ Агенты и чаты
- ✅ Настройки и конфигурация
- ❌ НЕ логотип Grafana

---

## ✅ ГОТОВО К ДЕПЛОЮ!

Вся предварительная работа выполнена:
1. ✅ Код скачан с GitHub
2. ✅ Зависимости установлены
3. ✅ Проект собран
4. ✅ Локально протестирован на порту 3002
5. ✅ UI работает корректно

**Можно деплоить в облако! 🚀**
