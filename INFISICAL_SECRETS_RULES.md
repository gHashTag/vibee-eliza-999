# 🕉️ ПРАВИЛА ЗАГРУЗКИ СЕКРЕТОВ ИЗ INFISICAL CLOUD

## ⚠️ КРИТИЧЕСКИ ВАЖНО: ЦЕНТРАЛИЗОВАННАЯ ЗАГРУЗКА СЕКРЕТОВ

**Проблема:** Ранее секреты загружались в разных местах кодовой базы, что приводило к:
- Дублированию кода
- Конфликтам при загрузке
- Сложности в отладке
- Невозможности централизованного управления

**Решение:** Создан централизованный сервис `InfisicalSecretLoader` в `packages/server/src/services/infisicalSecretLoader.ts`

---

## 📋 ПРАВИЛА ИСПОЛЬЗОВАНИЯ

### ✅ ОБЯЗАТЕЛЬНО:

1. **ВСЕГДА используйте централизованный сервис:**
   ```typescript
   import { loadInfisicalSecrets } from '@elizaos/server/services/infisicalSecretLoader';
   
   // При старте приложения
   const result = await loadInfisicalSecrets();
   
   if (!result.success) {
     console.error('Failed to load secrets:', result.errors);
     // Решайте, продолжать ли работу без секретов
   }
   ```

2. **Загружайте секреты ОДИН РАЗ при старте приложения:**
   - В `packages/server/src/entrypoint.ts` - точка входа сервера
   - ПЕРЕД инициализацией любых сервисов, которые используют секреты
   - ПЕРЕД подключением к базе данных

3. **Используйте singleton паттерн:**
   - Сервис автоматически использует singleton
   - Повторные вызовы `loadInfisicalSecrets()` не загружают секреты повторно
   - Используйте `forceReload: true` только для тестов

### ❌ ЗАПРЕЩЕНО:

1. **НЕ создавайте дублирующие функции загрузки секретов:**
   - ❌ НЕ создавайте новые функции `loadSecretsFromInfisical()`
   - ❌ НЕ используйте `InfisicalSDK` напрямую для загрузки секретов
   - ❌ НЕ дублируйте логику загрузки в разных местах

2. **НЕ загружайте секреты в нескольких местах:**
   - ❌ НЕ загружайте секреты в `packages/server/src/index.ts`
   - ❌ НЕ загружайте секреты в плагинах
   - ❌ НЕ загружайте секреты в сервисах

3. **НЕ используйте устаревшие методы:**
   - ❌ `packages/plugin-vibe-face-avatar/src/services/infisicalLoader.ts` - DEPRECATED
   - ❌ Любые другие функции загрузки секретов - DEPRECATED

---

## 🏗️ АРХИТЕКТУРА

### Централизованный сервис:

```
packages/server/src/services/infisicalSecretLoader.ts
├── InfisicalSecretLoader (класс)
│   ├── getInstance() - singleton
│   ├── loadSecrets() - загрузка секретов
│   └── reset() - сброс для тестов
└── loadInfisicalSecrets() - удобная функция
```

### Использование:

```
packages/server/src/entrypoint.ts
└── loadInfisicalSecrets() → устанавливает process.env
    └── Все остальные модули используют process.env
```

---

## 🔧 КОНФИГУРАЦИЯ

### Переменные окружения (обязательные):

```bash
INFISICAL_CLIENT_ID=88fcf0cd-cce9-4844-bad2-8e19b4bad3ed
INFISICAL_CLIENT_SECRET=b377e7a60b669ea2317f339dc6cb79ce49d588a7bbed92433bb2a73dedff3314
INFISICAL_PROJECT_ID=fd763fa3-35d5-4045-93bd-1795c5f00fc3
INFISICAL_ENVIRONMENT=dev  # или production, staging
```

### Определение environment:

1. Если указан `INFISICAL_ENVIRONMENT` - используется он
2. Если `NODE_ENV === 'production'` - используется `'production'`
3. Иначе - используется `'dev'`

---

## 📊 РЕЗУЛЬТАТ ЗАГРУЗКИ

### Интерфейс `SecretLoadResult`:

```typescript
interface SecretLoadResult {
  success: boolean;              // Успешна ли загрузка
  secretsLoaded: number;         // Количество загруженных секретов
  errors: string[];              // Массив ошибок
  criticalSecrets: {              // Статус критичных секретов
    [key: string]: 'loaded' | 'missing' | 'error';
  };
}
```

### Критичные секреты:

- `POSTGRES_URL` - подключение к базе данных
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `OPENROUTER_API_KEY` - API ключ OpenRouter
- `OPENAI_API_KEY` - API ключ OpenAI

---

## 🧪 ТЕСТИРОВАНИЕ

### Локальная проверка:

```bash
bun run scripts/test-infisical-postgres.ts
```

### Использование в тестах:

```typescript
import { InfisicalSecretLoader } from '@elizaos/server/services/infisicalSecretLoader';

// Перед тестами
const loader = InfisicalSecretLoader.getInstance();
await loader.loadSecrets(undefined, true); // forceReload = true

// После тестов
loader.reset();
```

---

## 🐛 ОТЛАДКА

### Логирование:

Сервис автоматически логирует:
- ✅ Успешную загрузку секретов
- ⚠️ Отсутствующие критичные секреты
- ❌ Ошибки загрузки
- 🔍 Детали подключения (хост, порт для POSTGRES_URL)

### Диагностика проблем:

1. **Секреты не загружаются:**
   - Проверьте переменные окружения (INFISICAL_*)
   - Проверьте логи на наличие ошибок
   - Проверьте доступность Infisical Cloud

2. **POSTGRES_URL не найден:**
   - Проверьте, что секрет установлен в Infisical Cloud
   - Проверьте правильность environment
   - Проверьте логи на наличие ошибок парсинга URL

3. **Ошибки подключения к базе данных:**
   - Проверьте, что POSTGRES_URL загружен: `process.env.POSTGRES_URL`
   - Проверьте доступность базы данных из сети
   - Проверьте настройки файрвола

---

## 📝 МИГРАЦИЯ СТАРОГО КОДА

### Было (❌ НЕПРАВИЛЬНО):

```typescript
// packages/server/src/entrypoint.ts
import { InfisicalSDK } from '@infisical/sdk';

const loadSecretsFromInfisical = async () => {
  // ... 100+ строк кода ...
};
```

### Стало (✅ ПРАВИЛЬНО):

```typescript
// packages/server/src/entrypoint.ts
import { loadInfisicalSecrets } from './services/infisicalSecretLoader.js';

const result = await loadInfisicalSecrets();
if (!result.success) {
  console.error('Failed to load secrets:', result.errors);
}
```

---

## 🎯 ПРИОРИТЕТЫ

1. **Безопасность:** Секреты загружаются один раз, централизованно
2. **Надежность:** Обработка ошибок, логирование, валидация
3. **Простота:** Один сервис, один способ использования
4. **Поддерживаемость:** Легко найти и исправить проблемы

---

## 🔄 ОБНОВЛЕНИЕ ПРАВИЛ

Если нужно изменить логику загрузки секретов:
1. Обновляйте ТОЛЬКО `packages/server/src/services/infisicalSecretLoader.ts`
2. Обновляйте эту документацию
3. НЕ создавайте новые функции загрузки секретов

---

**Последнее обновление:** 2025-11-29
**Автор:** Централизованная система загрузки секретов



