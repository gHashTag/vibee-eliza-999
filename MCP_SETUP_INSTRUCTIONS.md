# 🔧 MCP Setup - Инструкция по подключению Sentry

## ✅ Конфигурация готова

Файл `.claude/mcp.json` обновлен с относительными путями:
```json
{
  "mcpServers": {
    "sentry-mcp": {
      "command": "node",
      "args": ["./sentry-mcp-server.js"],
      "env": {
        "SENTRY_DSN": "...",
        "SENTRY_API_TOKEN": "..."
      }
    }
  }
}
```

## 🔄 Перезапуск MCP

### Способ 1: Перезапуск Claude Code

1. **Закройте Claude Code** полностью
2. **Подождите 10 секунд**
3. **Откройте снова** в папке `/Users/playra/vibee-eliza-999/`
4. **Откройте список MCP серверов**

### Способ 2: Перезапуск MCP серверов

В Claude Code:
1. Откройте MCP меню
2. Найдите `sentry-mcp`
3. Если есть кнопка "Restart" - нажмите
4. Или найдите "Reload" в настройках

### Способ 3: Проверить конфигурацию

```bash
# В терминале, в папке проекта
cd /Users/playra/vibee-eliza-999
ls -la .claude/mcp.json
cat .claude/mcp.json

# Тест MCP сервера
cd .claude
node sentry-mcp-server.js --test
```

## 📋 Что должно быть

После перезапуска в списке MCP:
```
sentry-mcp  ✔ connected
```

## 🚨 Если не появляется

Возможные причины:
1. **Claude Code не видит .claude/mcp.json**
   - Попробуйте создать файл `~/.config/claude/mcp.json`
   - Скопируйте туда конфигурацию из `.claude/mcp.json`

2. **Неправильные пути**
   - Проверьте что `node sentry-mcp-server.js --test` работает
   - Проверьте права файла: `chmod +x .claude/sentry-mcp-server.js`

3. **Claude не перезапустился**
   - Полностью закройте приложение
   - Удалите временные файлы: `rm -rf ~/.cache/claude*`
   - Запустите снова

## 📝 Альтернативный способ

Если MCP не работает, можно использовать вручную:
```bash
# Проверить последние ошибки
cd .claude
node sentry-mcp-server.js --latest-errors

# Поиск ошибок
node sentry-mcp-server.js --search 401
```

## ✅ Результат

После подключения можно использовать:
- `sentry-get-issues` - получить последние ошибки
- `sentry-search-issues` - поиск по ошибкам
- `sentry-test` - тест подключения

---

**Если ничего не помогает - перезапустите компьютер** 😄
