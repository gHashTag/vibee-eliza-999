# 🚂 Railway Deployment Guide

## 📋 Подготовка к деплою

**✅ Статус готовности:**
- ✅ Все 14 пакетов собраны успешно
- ✅ Код запушен в GitHub репозиторий
- ✅ Сервер тестирован локально на порту 3000
- ✅ База данных настроена (PostgreSQL + PGLite fallback)

## 🚀 Пошаговая инструкция деплоя

### 1️⃣ **Создать новый проект на Railway**

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**

### 2️⃣ **Подключить репозиторий**

1. В списке репозиториев найдите: `gHashTag/vibee-eliza-999`
2. Выберите **develop** ветку
3. Нажмите **"Deploy Now"**

### 3️⃣ **Настроить переменные окружения (Railway Variables)**

Railway автоматически определит Node.js проект и запустит:
```bash
npm install
npm run build
npm run start
```

**Обязательные переменные в Railway Dashboard → Variables:**

```env
# 🔐 REQUIRED: AI API Keys (минимум один)
OPENAI_API_KEY=your_openai_key_here

# 📊 Database (опционально - Railway предложит PostgreSQL)
# Если не указывать, будет использоваться PGLite (локальная БД)
POSTGRES_URL=postgresql://user:pass@host:5432/db

# 🔧 App Configuration
SERVER_PORT=3000
NODE_ENV=production

# 🧠 Infisical Cloud (optional - for secret management)
INFISICAL_CLIENT_ID=88fcf0cd-cce9-4844-bad2-8e19b4bad3ed
INFISICAL_CLIENT_SECRET=b377e7a60b669ea2317f339dc6cb79ce49d588a7bbed92433bb2a73dedff3314
INFISICAL_PROJECT_ID=fd763fa3-35d5-4045-93bd-1795c5f00fc3
INFISICAL_ENVIRONMENT=dev
```

### 4️⃣ **Дождаться деплоя**

Railway автоматически:
- ✅ Установит зависимости (`bun install`)
- ✅ Соберет проект (`bun run build`)
- ✅ Запустит сервер (`bun run start`)
- ✅ Дампнет URL типа: `https://your-app-production-xxx.up.railway.app`

## 🔍 Проверка деплоя

После успешного деплоя откройте URL и проверьте:

1. **Web Interface:** `https://xxx.up.railway.app`
2. **API Status:** `https://xxx.up.railway.app/api/status`
3. **API Agents:** `https://xxx.up.railway.app/api/agents`

## 📊 Мониторинг

- **Railway Dashboard:** 查看 логи в реальном времени
- **Logs:** В Railway → your project → Deployments → View Logs
- **Metrics:** CPU, RAM, Network usage

## 🎯 Обновление деплоя

Просто пушите в `develop` ветку:
```bash
git add .
git commit -m "Your changes"
git push origin develop
# Railway автоматически пересоберет!
```

## 🆘 Troubleshooting

### Проблема: Build failed
**Решение:** Проверьте логи в Railway Dashboard
- Убедитесь что все зависимости указаны
- Проверьте что скрипты build и start работают локально

### Проблема: Server crashed on startup
**Решение:**
- Проверьте переменные окружения
- Убедитесь что PORT=3000 установлен (Railway даст свой PORT автоматически)
- Добавьте `process.env.PORT` в `SERVER_PORT`

### Проблема: Database connection error
**Решение:**
- Если используете PostgreSQL - проверьте POSTGRES_URL
- Если нет - приложение автоматически использует PGLite (локальная БД)

## 💰 Стоимость

**Railway Pricing:**
- **Hobby Plan:** $0/month (free tier)
- **Pro Plan:** $20/month (production ready)
- **Usage:** Pay-as-you-go for resources

**Рекомендуется:** Hobby Plan для тестирования, Pro для продакшена.

## ✅ Готово!

После деплоя у вас будет:
- ✅ Публичный URL
- ✅ Автоматические деплойменты при git push
- ✅ Логи и мониторинг
- ✅ SSL сертификат (HTTPS)
- ✅ Масштабирование при нагрузке

**Удачного деплоя! 🎉**