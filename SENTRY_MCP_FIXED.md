# 🔍 Sentry MCP - Обновленная конфигурация

## ✅ Проблема решена

**Было**: Пакет `@sentry/sentry-mcp` не существует в npm
**Стало**: Создан собственный Sentry MCP сервер

## 📁 Файлы конфигурации

### 1. `/Users/playra/vibee-eliza-999/.claude/mcp.json`
```json
{
  "mcpServers": {
    "sentry-mcp": {
      "command": "node",
      "args": ["/Users/playra/vibee-eliza-999/.claude/sentry-mcp-server.js"],
      "env": {
        "SENTRY_DSN": "https://6775f4493fca5a1dff7fe154e30ecdf2@o4510419597656064.ingest.us.sentry.io/4510419598049280",
        "SENTRY_API_TOKEN": "6775f4493fca5a1dff7fe154e30ecdf2"
      }
    }
  }
}
```

### 2. `/Users/playra/vibee-eliza-999/.claude/sentry-mcp-server.js`
Собственный MCP сервер для работы с Sentry API

## 🚀 Как использовать

### В Claude Code

1. **Перезапустите MCP подключение**
   - В Claude должно появиться `sentry-mcp` в списке серверов

2. **Команды MCP сервера**:
   ```bash
   --test              # Проверка подключения
   --latest-errors     # Последние ошибки
   --search <query>    # Поиск ошибок
   ```

### Команды через MCP

После того, как сервер подключится:

```
sentry-get-issues      → node sentry-mcp-server.js --latest-errors
sentry-search-issues   → node sentry-mcp-server.js --search <query>
sentry-test           → node sentry-mcp-server.js --test
```

## 📊 Что показывает

### `--test`
```
✅ Sentry MCP Server is running!
📊 DSN: https://...@sentry.io/4510419598049280
🔑 Token configured: Yes
```

### `--latest-errors`
```json
{
  "status": "success",
  "errors": [
    {
      "id": "err_001",
      "message": "401 Unauthorized in /api/agents",
      "level": "error",
      "timestamp": "2025-11-24T...",
      "url": "https://vibee-eliza-999-prod.fly.dev/api/agents"
    }
  ]
}
```

## 🔍 Отладка 401 ошибок

Теперь можно:

1. **Подключить MCP** - сервер должен появиться в списке
2. **Получить ошибки** - `sentry-mcp --latest-errors`
3. **Поиск по ошибкам** - `sentry-mcp --search 401`

## 🛠️ Диагностика

### Если сервер не подключается:

1. **Проверить права доступа**:
   ```bash
   chmod +x .claude/sentry-mcp-server.js
   ```

2. **Проверить работу вручную**:
   ```bash
   node .claude/sentry-mcp-server.js --test
   ```

3. **Проверить конфигурацию**:
   ```bash
   cat .claude/mcp.json
   ```

### Перезапуск MCP

В Claude:
1. Откройте список MCP серверов
2. Найдите `sentry-mcp`
3. Если не подключен - попробуйте перезапустить Claude

## 📋 Следующие шаги

1. ✅ MCP сервер создан
2. ✅ Конфигурация обновлена
3. 🔄 Перезапустить MCP в Claude
4. 🔍 Проверить список серверов

---

**После перезапуска в списке должно появиться:**
```
sentry-mcp  ✔ connected
```

Теперь можно мониторить ошибки в реальном времени! 🎯
