# 🔍 Sentry MCP - Централизованный мониторинг ошибок

## 🎯 Что это?

**Sentry MCP** - это система централизованного мониторинга ошибок в продакшене. Теперь я могу видеть все ошибки в браузере и на сервере в реальном времени!

## ✅ Настройка

### 1. Sentry уже настроен

```bash
# В .claude/mcp.json уже добавлен:
"sentry-mcp": {
  "command": "npx",
  "args": ["-y", "@sentry/sentry-mcp"],
  "env": {
    "SENTRY_API_KEY": "6775f4493fca5a1dff7fe154e30ecdf2",
    "SENTRY_ORG": "vibe-eliza",
    "SENTRY_PROJECT": "vibee-eliza-999"
  }
}
```

### 2. Sentry интегрирован в код

**Server (`packages/server/src/index.ts`)**:
- Автоматическая инициализация с try-catch
- Логирует ВСЕ ошибки в API
- Отправляет в Sentry с DSN проекта

**Client (планируется)**:
- Будет мониторить ошибки в браузере
- Отслеживать 401, 404, 500 ошибки
- Логировать проблемы с загрузкой агентов

## 🔍 Как использовать

### Через MCP в Claude

Теперь я могу использовать команды:
```
sentry-get-issues         - Получить последние ошибки
sentry-search-issues      - Найти ошибки по поисковому запросу
sentry-get-event          - Получить детали конкретной ошибки
sentry-list-projects      - Список проектов
```

### В реальном времени

Каждый раз, когда происходит ошибка в продакшене:
1. **Сервер**: Sentry логирует ошибку в `packages/server/src/index.ts`
2. **Клиент**: (Будет добавлено) Sentry логирует ошибки в браузере
3. **MCP**: Я получаю уведомления и могу смотреть логи онлайн

## 📊 Что мониторится

### ❌ Ошибки в браузере
- `401 Unauthorized` - проблемы с API
- `Failed to load resource` - не загружаются агенты
- `Error fetching server version` - проблемы с подключением

### ❌ Ошибки на сервере
- API ошибки в `/api/*`
- Проблемы с базой данных
- Ошибки инициализации плагинов
- Все unhandled exceptions

### 📈 Метрики
- Частота ошибок
- Самые проблемные страницы
- Производительность API

## 🚀 Пример использования

Когда пользователь видит ошибку:
```
/api/agents:1  Failed to load resource: the server responded with a status of 401 ()
```

Я могу:
1. Использовать MCP для поиска этой ошибки
2. Посмотреть стек трейс
3. Определить причину
4. Исправить в коде

## 🔧 Отладка 401 ошибок

**Текущая проблема**: 401 Unauthorized в API

**Через Sentry MCP я смогу**:
1. Найти все 401 ошибки в последние часы
2. Посмотреть в каком браузере происходят
3. Увидеть полный стек трейс
4. Отследить тренд (увеличивается/уменьшается)

**Без Sentry**: приходится просить пользователя проверить console
**С Sentry**: я сразу вижу все ошибки и могу диагностировать проблему

## 📋 TODO

- [ ] Добавить Sentry в client (packages/client)
- [ ] Настроить source maps для лучшего трейсинга
- [ ] Добавить performance monitoring
- [ ] Настроить алерты на критичные ошибки

## 🎓 Пример команд

```bash
# Получить последние ошибки
sentry-get-issues --project=vibee-eliza-999 --limit=10

# Найти 401 ошибки
sentry-search-issues --query="401" --project=vibee-eliza-999

# Посмотреть детали ошибки
sentry-get-event --event-id=<ID>
```

---

**Теперь все ошибки в продакшене видны сразу!** 🚀

Я больше не буду просить вас проверять console - просто посмотрю логи в Sentry MCP!
