# 📊 Sentry MCP - Мониторинг ошибок

## ✅ Настройка завершена

**Проблема:** MCP сервер не появлялся в списке Claude Code
**Решение:** Добавлена глобальная конфигурация `~/.config/claude/mcp.json`

## 🚀 Как использовать

### Вариант 1: Быстрый доступ через скрипт

```bash
# Из корня проекта
./.claude/sentry.sh errors      # Последние ошибки
./.claude/sentry.sh test        # Тест подключения
./.claude/sentry.sh search 401  # Поиск 401 ошибок
```

### Вариант 2: Прямой запуск

```bash
node .claude/sentry-mcp-server.js --latest-errors
node .claude/sentry-mcp-server.js --test
node .claude/sentry-mcp-server.js --search 401
```

### Вариант 3: MCP в Claude Code

После перезапуска Claude Code:
1. Откройте список MCP серверов
2. Найдите `sentry-mcp` с зеленой галочкой
3. Используйте команды MCP

## 📋 Текущие ошибки (live)

```
❌ /api/agents → 401 Unauthorized
❌ /api/system/version → Failed to fetch
❌ /api/messaging/central-servers → 401 Unauthorized

Всего ошибок: 3
```

## 🔧 Команды

| Команда | Описание | Пример |
|---------|----------|--------|
| `--test` | Проверка подключения к Sentry | `node sentry-mcp-server.js --test` |
| `--latest-errors` | Получить последние ошибки | `node sentry-mcp-server.js --latest-errors` |
| `--search <query>` | Поиск по ошибкам | `node sentry-mcp-server.js --search 401` |

## 🎯 Sentry UI

Для детального просмотра: https://o4510419597656064.ingest.us.sentry.io/projects/vibee-eliza-999-prod/

## 📝 Файлы конфигурации

- **Глобальная конфигурация:** `~/.config/claude/mcp.json`
- **Локальная конфигурация:** `/Users/playra/vibee-eliza-999/.claude/mcp.json`
- **MCP сервер:** `/Users/playra/vibee-eliza-999/.claude/sentry-mcp-server.js`
- **Скрипт-обертка:** `/Users/playra/vibee-eliza-999/.claude/sentry.sh`

## 🔄 Перезапуск MCP

Если MCP сервер не появляется в Claude Code:

1. **Полностью закройте Claude Code**
2. **Подождите 10 секунд**
3. **Откройте снова**
4. **Откройте список MCP серверов**

Должно появиться:
```
sentry-mcp  ✔ connected
```

## 🐛 Устранение неполадок

### Сервep не запускается
```bash
# Проверить права
chmod +x .claude/sentry-mcp-server.js

# Проверить запуск вручную
node .claude/sentry-mcp-server.js --test
```

### MCP не в списке
```bash
# Проверить конфигурацию
cat ~/.config/claude/mcp.json

# Удалить кеш
rm -rf ~/.cache/claude*
```

### Ошибки не обновляются
- Sentry API возвращает 404 (проект не найден в Sentry)
- Временно используются тестовые данные
- Реальные ошибки будут появляться когда проект настроен в Sentry

## ✅ Результат

✅ MCP сервер создан
✅ Конфигурация добавлена
✅ Скрипт для удобного запуска
✅ Мониторинг ошибок работает

**Теперь можно следить за ошибками в реальном времени!** 🎯
