# 🔧 Исправление Telegram Login Widget

## ❌ Проблема
Виджет Telegram Login Widget загружается, но **не выполняет аутентификацию пользователей**.

## ✅ Решение

### Шаг 1: Привязка домена в BotFather (КРИТИЧНО!)

Домен сайта **ОБЯЗАТЕЛЬНО** должен быть привязан к боту через BotFather.

#### Действия:
1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду: `/setdomain`
4. Выберите бота: `agent_vibecoder_bot` (ID: 8309813696)
5. Укажите домен производственного сайта:
   ```
   vibee-eliza-999-prod-v2.fly.dev
   ```

#### ⚠️ ВАЖНО:
- Без этой привязки виджет НЕ БУДЕТ работать, даже если он загружается
- Нужно указать именно тот домен, где размещен сайт
- Если домен изменится, нужно будет обновить настройку

### Шаг 2: Проверка настройки

После привязки домена проверьте:
1. Откройте сайт в браузере
2. Откройте DevTools (F12)
3. Проверьте вкладку Console - должны быть логи загрузки виджета
4. Попробуйте войти через виджет

### Шаг 3: Диагностика

Если проблемы продолжаются, проверьте:

#### В Browser Console (DevTools):
```javascript
// Должно вывести тип функции (function)
typeof window.onTelegramAuth

// Должно показать DOM элемент виджета
document.querySelector('iframe[src*="telegram"]')
```

#### В Network Tab (DevTools):
- Ищите запросы к `telegram-widget.js`
- Проверьте, нет ли ошибок 403/404
- Убедитесь, что виджет загружается с `https://telegram.org`

#### В Server Logs:
- `/api/auth/login` должен получать POST запросы после авторизации
- Проверьте, что данные пользователя приходят в правильном формате

### Шаг 4: Код виджета

Текущая конфигурация виджета:
```html
<script src="https://telegram.org/js/telegram-widget.js?22"
        data-telegram-login="agent_vibecoder_bot"
        data-size="large"
        data-onauth="onTelegramAuth(user)"
        data-request-access="write"
        data-userpic="true">
</script>
```

### Шаг 5: Альтернативные домены

Для тестирования можно добавить дополнительные домены:
- `localhost` - для локальной разработки
- `127.0.0.1` - альтернативный localhost

### Шаг 6: Проверка токена бота

Убедитесь, что токен бота валиден:
- Получен из [@BotFather](https://t.me/BotFather)
- Сохранен в Infisical как `TELEGRAM_BOT_TOKEN`
- Настройки бота позволяют авторизацию через виджет

## 🔍 Часто задаваемые вопросы

**Q: Виджет отображается, но кнопка не работает**
A: Скорее всего домен не привязан в BotFather

**Q: Получаю ошибку в консоли браузера**
A: Проверьте CSP настройки сервера (должны разрешать `telegram.org`)

**Q: Callback не вызывается**
A: Убедитесь, что `window.onTelegramAuth` определена глобально

**Q: Авторизация проходит, но пользователь не входит в систему**
A: Проверьте серверный endpoint `/api/auth/login`

## 📝 Логи для отладки

При успешной работе вы должны увидеть:
```
🔔 [LOGINPAGE] Регистрируем onTelegramAuth callback в глобальной области
📦 [LOGINPAGE] Загружаем Telegram Login Widget...
✅ [LOGINPAGE] Скрипт виджета загружен успешно
🔔 [LOGINPAGE] === ПОЛУЧЕНЫ ДАННЫЕ ОТ TELEGRAM ===
🔐 [CLIENT] Начинаем процесс логина...
```

## 🚀 После исправления

1. Задеплойте изменения
2. Проверьте работу виджета на продакшене
3. Убедитесь, что авторизация работает
4. Удалите кнопку перезагрузки виджета из кода (для продакшена)

## 📞 Поддержка

Если проблема не решается:
1. Проверьте все логи в DevTools
2. Убедитесь в правильности настройки BotFather
3. Проверьте, что домен точно совпадает (включая протокол)
4. Обратитесь к документации Telegram: https://core.telegram.org/widgets/login
