import { Character } from '@elizaos/core';
import { instagramAgent } from './instagram';

/**
 * Instagram Expert Character
 * Экспортирует instagramAgent как экспорт по умолчанию
 * для совместимости с elizaos CLI
 */
export default instagramAgent as Character;
