# 🎭 KOLS AGENT - Мониторинг групповых чатов Telegram

## 📋 Описание

**KOLS AGENT** - это персонаж для VIBEE агента, специализирующийся на мониторинге групповых чатов в Telegram в **реальном времени**.

### ⚡ Возможности

- ✅ **Прослушивание всех сообщений** во всех группах где аккаунт участник
- ✅ **Автоматическая реакция на триггерные слова** (help, report, urgent и др.)
- ✅ **Статистика мониторинга** (количество групп, сообщений, uptime)
- ✅ **Фильтрация по группам** (добавление/удаление из списка мониторинга)
- ✅ **Поддержка медиа** (фото, видео, документы)
- ✅ **GramJS (MTProto)** для полного функционала userbot

## 🏗️ Архитектура

```
KOLS AGENT
├── plugin-telegram-craft/          # Плагин для работы с Telegram
│   ├── actions/
│   │   ├── startGroupMonitoring.action.ts    # Запуск мониторинга
│   │   └── addGroupToMonitor.action.ts       # Добавление групп
│   ├── services/
│   │   ├── telegram.service.ts              # Основной сервис
│   │   └── adapters/
│   │       └── mtproto.adapter.ts           # MTProto адаптер (GramJS)
│   └── types/telegram.types.ts              # Типы TypeScript
├── src/characters/
│   └── kolsAgent.character.ts               # Персонаж KOLS AGENT
└── scripts/                                 # Скрипты для тестирования
    ├── demo-monitoring.sh                   # Демонстрация
    ├── simulate-messages.js                 # Симуляция сообщений
    ├── monitor-logs.sh                      # Мониторинг логов
    └── test-monitoring.sh                   # Интерактивные тесты
```

## 🚀 Быстрый старт

### 1. **Запуск агента**

```bash
cd /Users/playra/vibee-agent

# Установить зависимости
npm install

# Запустить в режиме разработки
npm run dev

# Или в hot reload режиме (рекомендуется)
npm run dev:hot
```

### 2. **Настройка Telegram API** (для реального подключения)

Добавьте в `.env.dev`:

```bash
# Обязательно для MTProto (GramJS)
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
TELEGRAM_SESSION_STRING=ваш_session_string
TELEGRAM_PHONE=ваш_номер_телефона
TELEGRAM_PASSWORD=ваш_пароль_2fa

# Или используйте Bot API (ограниченный функционал)
TELEGRAM_BOT_TOKEN=ваш_bot_token

# Стратегия подключения
TELEGRAM_STRATEGY=mtproto  # или botapi
```

### 3. **Команды в Telegram**

После запуска агента отправьте боту команды:

```
/monitor start                    # Запустить мониторинг
добавь группу @название           # Добавить группу в наблюдение
статистика мониторинга            # Показать статистику
покажи группы                    # Список мониторимых групп
```

## 🧪 Тестирование

### Интеграционные тесты

```bash
cd plugin-telegram-craft
bun test src/__tests__/group-monitoring.test.ts
```

**Результат:** ✅ 7 тестов прошло

### Демонстрация работы

```bash
# Полная демонстрация с симуляцией сообщений
./scripts/demo-monitoring.sh

# Только мониторинг логов
./scripts/monitor-logs.sh

# Интерактивное тестирование
./scripts/test-monitoring.sh

# Симуляция сообщений (без агента)
node scripts/simulate-messages.js
```

## 📊 Возможности мониторинга

### 🔍 Что отслеживается

- **Все сообщения** во всех группах где аккаунт участник
- **Триггерные слова:**
  - `help`, `помощь` - запрос помощи
  - `report`, `пожаловаться` - жалобы
  - `urgent`, `срочно` - срочные вопросы
- **Медиа файлы:** фото, видео, документы, голосовые
- **Статистика активности:** количество сообщений, uptime

### 📈 Статистика

```
📊 Статистика мониторинга:
• Всего групп: 3
• Активных групп: 3
• Сообщений обработано: 127
• Время работы: 2ч 15м
```

### 🔔 Реакция на триггеры

При обнаружении триггерного слова:

```
⚠️ Триггерные слова найдены: help, urgent
📍 В чате: Разработчики Node.js
👤 От: Александр (12345)
💬 Сообщение: Help! Не могу подключиться к базе данных...
```

## 📝 Примеры использования

### 1. **Мониторинг группы разработчиков**

```bash
# В Telegram отправить:
"запусти мониторинг групп"
# Агент ответит:
# ✅ Мониторинг запущен!
# 📊 Статистика:
# • Всего групп: 0
# • Активных групп: 0
```

### 2. **Добавление группы**

```bash
# В Telegram отправить:
"добавь группу @dev_team"
# Агент ответит:
# ✅ Группа "@dev_team" добавлена в мониторинг!
```

### 3. **Просмотр статистики**

```bash
# В Telegram отправить:
"статистика мониторинга"
# Агент покажет:
# 📊 Статистика мониторинга:
# • Всего групп: 5
# • Активных групп: 5
# • Сообщений обработано: 342
# • Время работы: 5ч 23м
```

## 🔧 Настройка

### Переменные окружения

| Переменная | Обязательно | Описание |
|------------|-------------|----------|
| `TELEGRAM_API_ID` | Да | API ID из my.telegram.org |
| `TELEGRAM_API_HASH` | Да | API Hash из my.telegram.org |
| `TELEGRAM_SESSION_STRING` | Нет | Сессия для авторизации |
| `TELEGRAM_PHONE` | Нет | Номер телефона |
| `TELEGRAM_PASSWORD` | Нет | Пароль 2FA |
| `TELEGRAM_BOT_TOKEN` | Альтернатива | Токен бота |
| `TELEGRAM_STRATEGY` | Нет | mtproto/botapi (по умолчанию mtproto) |

### Триггерные слова

Редактируйте в `plugin-telegram-craft/src/services/telegram.service.ts`:

```typescript
private checkTriggerWords(message: any): void {
  const triggerWords = [
    'help',
    'помощь',
    'пожаловаться',
    'report',
    'urgent',
    'срочно',
    // Добавьте свои слова
  ];
  // ...
}
```

## 📚 Технологии

- **ElizaOS** - платформа для AI агентов
- **GramJS** (`telegram` npm package) - MTProto клиент
- **TypeScript** - типизированный JavaScript
- **Bun** - быстрый JavaScript runtime
- **MTProto** - протокол Telegram для userbot

## ⚠️ Важные замечания

### Безопасность

- **Никогда не коммитьте** API ключи в git
- Используйте `.env.dev` для разработки
- Для продакшена используйте Infisical (как в проекте)

### Лимиты Telegram

- **MTProto:** до 30 сообщений/сек на аккаунт
- **Bot API:** до 30 сообщений/сек в одну группу
- **Rate Limits:** соблюдайте лимиты для избежания блокировки

### Производительность

- Мониторинг работает в **реальном времени**
- Сообщения обрабатываются **асинхронно**
- Статистика обновляется **автоматически**

## 🐛 Troubleshooting

### Агент не видит сообщения

1. Проверьте подключение к Telegram:
   ```bash
   grep "Connected" agent.log
   ```

2. Убедитесь что аккаунт участник групп

3. Проверьте стратегию подключения:
   ```bash
   grep "Стратегия" agent.log
   ```

### Ошибки авторизации

```
❌ Auth error: Phone number invalid
```

**Решение:** Проверьте `TELEGRAM_PHONE` в `.env.dev`

```
❌ Auth error: Password is invalid
```

**Решение:** Проверьте `TELEGRAM_PASSWORD` в `.env.dev`

### Мониторинг не запускается

```
❌ Telegram сервис не инициализирован
```

**Решение:** Перезапустите агента `npm run dev`

## 📄 Лицензия

MIT License - см. файл LICENSE

## 🤝 Поддержка

- GitHub Issues: [создать issue](https://github.com/your-repo/issues)
- Документация: [GramJS docs](https://gram.js.org/)
- Telegram: [@your_username](https://t.me/your_username)

---

**🎉 KOLS AGENT готов к мониторингу ваших групповых чатов!**
