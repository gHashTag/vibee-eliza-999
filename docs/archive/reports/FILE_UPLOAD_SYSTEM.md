# 📤 Система загрузки файлов VIBEE

## Обзор

Система загрузки файлов VIBEE позволяет пользователям загружать файлы через веб-интерфейс в Supabase Storage, которые затем могут быть использованы Instagram плагином для публикации постов.

## Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                    Веб-интерфейс                          │
│  ┌──────────────────┐    ┌──────────────────┐            │
│  │  React Component │    │  Drag & Drop UI  │            │
│  │  (FileUpload)    │    │                  │            │
│  └────────┬─────────┘    └────────┬─────────┘            │
└───────────┼────────────────────────┼─────────────────────┘
            │                        │
            │ uploadFile()           │
            ▼                        ▼
┌─────────────────────────────────────────────────────────┐
│              Supabase Storage (Frontend)                 │
│  - bucket: 'instagram-uploads'                           │
│  - public access via RLS                                │
│  - auto-generated unique filenames                      │
└─────────────────────────────────────────────────────────┘
            │
            │ file URLs
            ▼
┌─────────────────────────────────────────────────────────┐
│              Instagram Plugin (Backend)                  │
│  ┌──────────────────┐   ┌─────────────────┐             │
│  │  StorageService  │   │ Instagram API   │             │
│  │  (Service Role)  │   │                 │             │
│  └────────┬─────────┘   └────────┬────────┘             │
└───────────┼────────────────────────┼─────────────────────┘
            │                        │
            │ URL → Buffer          │ Publish to Instagram
            ▼                        ▼
┌─────────────────────────────────────────────────────────┐
│              Instagram Business API                      │
└─────────────────────────────────────────────────────────┘
```

## Компоненты системы

### 1. **Frontend: React FileUpload Component**
- **Файл**: `src/frontend/components/FileUpload.tsx`
- **Функции**:
  - Drag & Drop интерфейс
  - Выбор файлов через диалог
  - Визуальные индикаторы прогресса
  - Предпросмотр изображений
  - Удаление файлов

### 2. **Frontend: Supabase Client**
- **Файл**: `src/frontend/supabase.ts`
- **Функции**:
  - Создание Supabase клиента
  - Загрузка файлов в Storage
  - Удаление файлов
  - Получение публичных URL

### 3. **Backend: StorageService**
- **Файл**: `plugin-vibe-instagram/src/services/storageService.ts`
- **Функции**:
  - Инициализация Supabase с Service Role Key
  - Загрузка файлов из URL
  - Автоочистка старых файлов (24h)
  - Bucket: `instagram-uploads`

### 4. **Backend: Instagram Action**
- **Файл**: `plugin-vibe-instagram/src/actions/instagramPostAction.ts`
- **Функции**:
  - Обработка команд публикации
  - Автоматическая загрузка изображений в Supabase
  - Публикация в Instagram

## Процесс загрузки файла

### Шаг 1: Загрузка через веб-интерфейс
```
Пользователь перетаскивает файл → React компонент → Supabase Storage
```

1. Пользователь перетаскивает файл в зону загрузки
2. React компонент создает уникальное имя файла: `{timestamp}-{random}.{ext}`
3. Файл загружается в Supabase Storage bucket `instagram-uploads`
4. Supabase возвращает публичный URL
5. Файл появляется в списке загруженных

### Шаг 2: Использование в Instagram
```
Пользователь отправляет URL → Instagram Action → Публикация
```

1. Пользователь отправляет URL файла боту
2. Instagram Action проверяет URL
3. StorageService скачивает файл в буфер
4. Файл публикуется в Instagram через Business API

## Конфигурация

### Переменные окружения

#### Backend (Infisical Cloud)
```bash
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key  # Для StorageService
```

#### Frontend (.env)
```bash
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key              # Для загрузки файлов
```

### Supabase Storage настройки

1. **Bucket**: `instagram-uploads`
   - Public access: да (для прямых загрузок)
   - RLS (Row Level Security): включен
   - Автоочистка: каждые 24 часа

2. **Политики доступа (RLS)**:
   ```sql
   -- Разрешить всем загружать файлы
   INSERT INTO storage.policies (bucket_id, name, definition)
   VALUES ('instagram-uploads', 'Allow uploads', 'true');

   -- Разрешить всем просматривать файлы
   INSERT INTO storage.policies (bucket_id, name, definition)
   VALUES ('instagram-uploads', 'Allow public access', 'true');
   ```

## Использование

### Загрузка файлов

1. Откройте веб-интерфейс агента
2. Перейдите на вкладку "File Upload"
3. Перетащите файлы в зону загрузки
4. Дождитесь появления зеленой галочки ✅
5. Скопируйте URL файла

### Публикация в Instagram

```bash
# Вариант 1: С URL файла
Опубликуй пост в Instagram https://your-project.supabase.co/storage/v1/object/public/instagram-uploads/1234567890-abc.jpg с подписью красивый закат #sunset

# Вариант 2: Отправить фото напрямую в Telegram
Пришлите фото боту с сообщением "Опубликуй это в Instagram"
```

## Автоочистка

Система автоматически удаляет файлы старше 24 часов:
- **Частота**: каждые 60 минут
- **Критерий**: файлы с timestamp > 24h
- **Лог**: записи в консоли о количестве удаленных файлов

## Файлы и директории

```
/Users/playra/vibee-agent/
├── src/frontend/
│   ├── components/
│   │   ├── FileUpload.tsx          # React компонент загрузки
│   │   └── FileUpload.css          # Стили компонента
│   ├── supabase.ts                 # Supabase клиент для фронтенда
│   └── index.tsx                   # Экспорт панелей интерфейса
├── plugin-vibe-instagram/
│   ├── src/
│   │   ├── services/
│   │   │   ├── storageService.ts   # Backend Supabase сервис
│   │   │   └── instagramService.ts # Instagram API сервис
│   │   ├── actions/
│   │   │   ├── instagramPostAction.ts  # Действие публикации
│   │   │   └── fileUploadAction.ts     # Действие загрузки
│   │   └── index.ts                # Экспорт плагина
└── .env.example                    # Шаблон переменных окружения
```

## Ограничения

1. **Размер файла**: до 10 МБ (настраивается в Supabase)
2. **Типы файлов**: изображения, видео, документы
3. **Хранение**: 24 часа (автоочистка)
4. **Публичность**: файлы доступны по прямому URL

## Устранение неполадок

### Ошибка: "Failed to fetch"
**Причина**: Неверный VITE_SUPABASE_URL или VITE_SUPABASE_ANON_KEY
**Решение**: Проверьте переменные в .env файле

### Ошибка: "Bucket not found"
**Причина**: Bucket `instagram-uploads` не создан
**Решение**: Создайте bucket в Supabase Dashboard

### Ошибка: "The resource already exists"
**Причина**: Файл с таким именем уже существует
**Решение**: Система генерирует уникальные имена, ошибка не должна возникать

### Файлы не удаляются автоматически
**Причина**: Ошибка в cleanup scheduler
**Решение**: Проверьте логи плагина, убедитесь что StorageService инициализирован

## Логи

Все операции логируются с префиксом:
- `[Supabase]` - операции фронтенда
- `[FileUpload]` - React компонент
- `[StorageService]` - Backend сервис
- `[Instagram]` - Плагин

Пример:
```
✅ [Supabase] Файл загружен: https://.../instagram-uploads/123.jpg
✅ [FileUpload] Файл загружен: https://.../instagram-uploads/123.jpg
✅ [StorageService] Upload result: { url: "...", path: "..." }
🐝 [Instagram] Инициализация Instagram плагина...
```

## Безопасность

1. **Frontend**: Использует anon key (ограниченные права)
2. **Backend**: Использует service role key (полные права)
3. **RLS**: Row Level Security настроен на bucket
4. **Автоочистка**: Предотвращает накопление файлов
5. **Уникальные имена**: Файлы именуются по timestamp + random

## Развитие

### Возможные улучшения
- [ ] Сжатие изображений перед загрузкой
- [ ] Генерация превью разных размеров
- [ ] Поддержка загрузки нескольких файлов сразу
- [ ] Интеграция с другими плагинами (не только Instagram)
- [ ] CDN для быстрой доставки файлов
- [ ] Watermark на изображения
- [ ] Expiration date для файлов (custom)
