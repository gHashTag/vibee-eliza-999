# 🔧 ПЛАН ИСПРАВЛЕНИЯ: ОГРАНИЧЕНИЕ ПО ГРУППЕ

## ❌ ЧТО БЫЛО НЕПРАВИЛЬНО:
1. Агент читал сообщения из ВСЕХ групп где состоит аккаунт:
   - "на Вайбе / фанчатик" (1957396718) ✅
   - "Android Developers" (1055466536) ❌
   - "Telegram Bot Talk" (1311056733) ❌

2. Нет фильтра по ID группы
3. Нет ограничений в коде

## ✅ ЧТО НУЖНО СДЕЛАТЬ:

### 1. Добавить конфигурацию ALLOWED_GROUP_ID
```typescript
// В .env или .infisical.env
ALLOWED_GROUP_ID=1957396718
```

### 2. Добавить фильтр в handleIncomingMessage()
```typescript
// Проверка перед обработкой
if (chatId !== ALLOWED_GROUP_ID) {
  console.log(`⏭️ Игнорируем сообщение из чужой группы: ${chatId}`);
  return;
}
```

### 3. Добавить фильтр в sendMessage()
```typescript
// Проверка перед отправкой
if (chatId !== ALLOWED_GROUP_ID) {
  console.log(`⛔ БЛОКИРОВКА: Попытка писать в чужую группу: ${chatId}`);
  return;
}
```

### 4. Пересобрать и перезапустить
```bash
bun run build
npm run dev
```

## 🎯 РЕЗУЛЬТАТ:
- Агент будет работать ТОЛЬКО в группе 1957396718
- Сообщения из других групп будут игнорироваться
- Ответы будут отправляться ТОЛЬКО в разрешённую группу
