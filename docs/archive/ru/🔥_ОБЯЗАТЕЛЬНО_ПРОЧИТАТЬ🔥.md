# 🔥 ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ! 🔥

## 🚨 КРИТИЧЕСКИ ВАЖНО! 🚨

### ✅ ЕДИНСТВЕННАЯ КОМАНДА ЗАПУСКА:

```bash
bun dev
```

### ❌ ЗАПРЕЩЕННЫЕ КОМАНДЫ:

```bash
npm run dev:hot
npm run dev
elizaos dev
bash scripts/dev-with-infisical.sh
```

## 📍 МЕСТО ЗАПУСКА:

```bash
# ТОЛЬКО из корня проекта:
cd /Users/playra/vibee-agent

# ТОЛЬКО эта команда:
bun dev
```

## 🎯 ПОЧЕМУ ЭТО ВАЖНО:

1. **`bun dev`** - правильный запуск (оба агента, Infisical, переменные)
2. **`elizaos dev`** - плагин режим (только один плагин, ошибки БД)
3. **`npm run dev:hot`** - НЕПРАВИЛЬНО (ломает загрузку переменных!)

## 📁 СТРУКТУРА:

```
/Users/playra/vibee-agent/          ← ЗДЕСЬ!
├── src/
│   ├── index.ts                    ← 2 агента
│   ├── character.ts                ← VIBEE
│   └── instagram-plugin/           ← Instagram
└── .env.dev                        ← Токены
```

## 🚀 ЗАПУСК ПРЯМО СЕЙЧАС:

```bash
cd /Users/playra/vibee-agent && bun dev
```

## 🎉 РЕЗУЛЬТАТ:

```
✅ VIBEE агент работает
✅ Instagram Expert агент работает
✅ Оба агента доступны в Telegram
✅ Instagram токены загружены
```

---

## 💀 ПОМНИ!

### ⚠️ ЕСЛИ ЗАПУСТИЛИ НЕПРАВИЛЬНО:
```bash
# ОСТАНОВИТЬ ВСЕ
pkill -f "elizaos"
pkill -f "bun dev"

# ПЕРЕЗАПУСТИТЬ ПРАВИЛЬНО
cd /Users/playra/vibee-agent && bun dev
```

---

## 🔥 ИТОГ:

**ОДНА КОМАНДА НА ВСЕ ВРЕМЕНА:**

```bash
bun dev
```

**ИЗ ОДНОГО МЕСТА:**

```bash
/Users/playra/vibee-agent
```

---
**НЕ ЗАБЫВАЙТЕ!** 🔥
