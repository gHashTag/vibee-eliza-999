# 📸 Instagram Plugin - Полная документация

## 📋 Обзор

Instagram плагин для VIBEE позволяет автоматически публиковать посты в Instagram через Meta Business API. Поддерживает публикацию изображений с подписями и хэштегами.

## 🏗️ Архитектура плагина

```
src/instagram-plugin/
├── index.ts                    # Главный файл плагина
├── actions/
│   └── instagramPostAction.ts  # Действие для публикации постов
├── services/
│   └── instagramService.ts     # Сервис для работы с Instagram API
├── types/
│   └── index.ts                # TypeScript типы и схемы валидации
└── README.md                   # Документация плагина
```

## 🔌 Подключение плагина

### В character.ts

```typescript
import { instagramPlugin } from './instagram-plugin/index';

export const instagramAgent: Character = {
  // ... другие настройки

  plugins: [
    instagramPlugin as any,
    "@elizaos/plugin-sql",
    "@elizaos/plugin-bootstrap",
  ],

  // ... остальные настройки
};
```

## 🔐 Настройка токенов

### ⚠️ ВАЖНО: Используйте Infisical Cloud!

Все токены должны храниться в Infisical, а не в .env файлах.

#### Переменные в Infisical:

1. **INSTAGRAM_ACCESS_TOKEN** - Long-lived токен доступа (60 дней)
2. **INSTAGRAM_ACCOUNT_ID** - ID Instagram бизнес-аккаунта

#### Получение токенов:

```bash
# 1. Создайте Meta App в https://developers.facebook.com/
# 2. Настройте Instagram Basic Display API или Instagram Graph API
# 3. Получите long-lived токен (60 дней)
# 4. Добавьте токены в Infisical

# Проверка токена
curl -X GET "https://graph.facebook.com/v18.0/me?access_token=YOUR_TOKEN"
```

#### Автоматическое обновление токенов:

Скрипт для обновления токенов каждые 60 дней должен быть настроен в Infisical.

## 💡 Использование

### Команды для пользователей:

```
Опубликуй пост в Instagram https://example.com/image.jpg с подписью Мой пост #vibee
```

```
Опубликуй это в Instagram с подписью Тестовый пост
```

```
/instagram красивый закат https://example.com/sunset.jpg #sunset #nature
```

### Как это работает:

1. **Парсинг сообщения** - функция `parseInstagramPost()` извлекает:
   - URL изображения
   - Подпись к посту
   - Хэштеги

2. **Валидация** - проверка через Zod схему `InstagramPostSchema`

3. **Публикация** - Instagram API вызовы:
   - Создание media объекта
   - Публикация в ленту

4. **Отчет** - отправка результата пользователю

## 🔍 Анализ функциональности

### Действие: `INSTAGRAM_POST`

**Trigger words:**
- `instagram`
- `пост`
- `опубликовать`
- `/instagram`

**Процесс:**

1. **Валидация входных данных**
   ```typescript
   const text = message.content.text?.toLowerCase() || '';
   return text.includes('instagram') ||
          text.includes('пост') ||
          text.includes('опубликовать') ||
          text.includes('/instagram');
   ```

2. **Парсинг сообщения**
   ```typescript
   const postData = parseInstagramPost(message.content.text, message.content.attachments);
   ```

3. **Извлечение данных:**
   - Поиск URL в тексте
   - Поиск изображений в attachments (Telegram файлы)
   - Извлечение подписи
   - Парсинг хэштегов

4. **Валидация через Zod**
   ```typescript
   const validation = InstagramPostSchema.safeParse(postData);
   ```

5. **Публикация через API**
   ```typescript
   const result = await instagramService.publishPost(validation.data);
   ```

### Сервис: `InstagramAPIService`

**Методы:**

- `publishPost(post)` - публикация поста
- `publishStory(story)` - публикация истории
- `getMetrics(metrics)` - получение метрик
- `getAccountInfo()` - информация об аккаунте
- `getMediaLibrary(limit)` - медиатека
- `checkConnection()` - проверка подключения

**Поток публикации:**

1. Создание media объекта:
   ```typescript
   POST /{ig-user-id}/media
   {
     "image_url": "...",
     "caption": "...",
     "access_token": "..."
   }
   ```

2. Публикация:
   ```typescript
   POST /{ig-user-id}/media_publish
   {
     "creation_id": "{media-id}",
     "access_token": "..."
   }
   ```

## 🐛 Устранение неполадок

### Проблема: "Instagram API токены не настроены"

**Решение:**
1. Проверьте наличие переменных в Infisical:
   ```
   INSTAGRAM_ACCESS_TOKEN
   INSTAGRAM_ACCOUNT_ID
   ```
2. Убедитесь, что токен не истек (60 дней)

### Проблема: "Не удалось опубликовать пост"

**Причины:**
- Неверный URL изображения
- Изображение недоступно
- Недостаточно прав у токена
- Ошибка API Instagram

**Решение:**
1. Проверьте URL изображения (должен быть публично доступен)
2. Проверьте права токена (Instagram Basic Display API)
3. Проверьте логи для детальной ошибки

### Проблема: "Прикрепите изображение"

**Решение:**
- Прикрепите файл изображения в Telegram
- ИЛИ укажите URL изображения в тексте сообщения

## 📊 Типы и схемы

### InstagramPost
```typescript
{
  caption: string;        // Подпись (max 2200 символов)
  imageUrl?: string;      // URL изображения
  mediaType: 'IMAGE' | 'VIDEO' | 'CAROUSEL';
  hashtags?: string[];    // Хэштеги
  locationId?: string;    // ID локации (опционально)
}
```

### InstagramStory
```typescript
{
  mediaUrl: string;       // URL медиа
  mediaType: 'IMAGE' | 'VIDEO';
  caption?: string;       // Подпись (max 300 символов)
}
```

## 🧪 Тестирование

### Проверка подключения:

```typescript
const service = runtime.getService('instagram-api');
const isConnected = await service.checkConnection();
```

### Отправка тестового поста:

```
Опубликуй пост в Instagram https://picsum.photos/600 с подписью Тестовый пост #test
```

## 🔄 Дублирование плагинов (ИСПРАВЛЕНО)

### Было (дублирование):

```
❌ /plugin-vibe-instagram/                              - дубликат
❌ /plugin-vibe-face-avatar/plugin-vibe-instagram/     - дубликат
✅ /src/instagram-plugin/                               - актуальный
```

### Стало (очищено):

```
✅ /src/instagram-plugin/                               - единственный плагин
```

**Удалено дублирование:**
- Удален `/plugin-vibe-instagram/`
- Удален `/plugin-vibe-face-avatar/plugin-vibe-instagram/`

## 📈 Возможности расширения

### 1. Публикация каруселей
```typescript
const carouselPost: InstagramPost = {
  mediaType: 'CAROUSEL',
  imageUrls: ['url1', 'url2', 'url3'],
  caption: 'Моя карусель',
};
```

### 2. Публикация с локацией
```typescript
const postWithLocation: InstagramPost = {
  imageUrl: '...',
  caption: '...',
  locationId: '123456789',
};
```

### 3. Публикация историй (Stories)
```typescript
const story: InstagramStory = {
  mediaUrl: '...',
  mediaType: 'IMAGE',
  caption: 'Моя история',
};
```

## 🚀 Следующие шаги

### Для разработчиков:

1. **Добавить поддержку видео** - расширить парсер для видео
2. **Добавить Stories API** - интегрировать публикацию историй
3. **Добавить метрики** - отслеживание engagement постов
4. **Добавить автопостинг** - расписание публикаций
5. **Добавить фильтры** - обработка изображений перед публикацией

### Для пользователей:

1. Настройте токены в Infisical
2. Протестируйте публикацию тестового поста
3. Настройте автоматическое обновление токенов
4. Используйте плагин через команды бота

## 📝 Логи и отладка

### Включение подробных логов:

```bash
LOG_LEVEL=debug npm run dev
```

### Проверка логов:

```bash
pm2 logs vibee-agent | grep -i instagram
```

### Основные логи:

- `✅ Пост опубликован в Instagram:` - успешная публикация
- `❌ Ошибка публикации в Instagram:` - ошибка API
- `⚠️ INSTAGRAM_ACCESS_TOKEN не найден` - нет токена

## 🎯 Заключение

Instagram плагин позволяет:
- ✅ Публиковать посты с изображениями
- ✅ Добавлять подписи и хэштеги
- ✅ Поддерживать Telegram attachments
- ✅ Валидировать данные через Zod
- ✅ Работать через Infisical Cloud

**Статус:** ✅ Работает (после очистки дублирования)

**Автор:** VIBEE Team
**Версия:** 1.0.0
**Дата:** 2025-11-22
