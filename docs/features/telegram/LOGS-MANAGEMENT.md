# 📊 ЛОГИРОВАНИЕ VIBEE - ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ

> **🎯 Цель**: Централизованная система логирования с цветовой инфографикой для удобного мониторинга и отладки VIBEE агента.

---

## 🎨 ЦВЕТОВАЯ СИСТЕМА ЛОГИРОВАНИЯ

### ANSI Цвета (в коде)
```typescript
// Определение цветов в telegram.service.ts:8-21
const colors = {
  reset: '\x1b[0m',
  bright: '\x1b[1m',

  // Основные цвета
  cyan: '\x1b[36m',      // 🔵 Группы/чаты
  yellow: '\x1b[33m',    // 🟡 Имена пользователей
  white: '\x1b[37m',     // ⚪ Текст сообщений
  green: '\x1b[32m',     // 🟢 Успех
  red: '\x1b[31m',       // 🔴 Ошибки
  magenta: '\x1b[35m',   // 🟣 Метаданные
  blue: '\x1b[34m',      // 🔷 Системные сообщения
  gray: '\x1b[90m',      // ⚫ Вспомогательная инфо
}
```

### Эмодзи-префиксы (в логах)
| Эмодзи | Значение | Пример использования |
|--------|----------|---------------------|
| 🔔 | Входящее сообщение | `🔔 [MTProto] New message event received!` |
| 📝 | Обработка текста | `📝 [MTProto] Message text: ...` |
| 💬 | Информация о чате | `💬 [MTProto] Chat: Название группы` |
| 🎯 | Обработка | `🎯 [TelegramService] handleIncomingMessage() called!` |
| 🤖 | ИИ-операции | `🤖 [TelegramService] Generating reply...` |
| 📤 | Отправка | `📤 [MTProto] Calling handler...` |
| 📥 | Получение | `📥 [TelegramService] Adapter handler called` |
| ✅ | Успех | `✅ Connected to Telegram!` |
| ❌ | Ошибка | `❌ Failed to send message` |
| ⚠️ | Предупреждение | `⚠️ messageManager not available` |
| 🔥 | Автозапуск | `🔥 [TelegramService] Auto-starting group monitoring` |
| 🚀 | Запуск | `🚀 [TelegramService] start() called` |
| 🔗 | Подключение | `🔗 Connecting to Telegram via MTProto` |
| 🔑 | Аутентификация | `🔑 Using existing session string` |
| 📡 | Статус | `📡 Стратегия: mtproto` |

---

## 📂 ФАЙЛЫ ЛОГОВ

### Основные лог-файлы
```
/Users/playra/vibee-agent/
├── 📄 agent.log              # ОСНОВНОЙ лог агента (ВСЕГДА использовать)
├── 📄 final-test.log         # Лог финального тестирования
├── 📄 rainbow-test.log       # Лог Rainbow Bridge тестов
└── 📄 test-instagram.log     # Лог Instagram плагина
```

### Ротация логов
```bash
# Архивировать старые логи
mv agent.log agent-$(date +%Y%m%d-%H%M%S).log

# Сжать архив
gzip agent-*.log

# Очистить архивы старше 7 дней
find . -name "agent-*.gz" -mtime +7 -delete
```

---

## 🛠️ СКРИПТЫ МОНИТОРИНГА

### 1. Основной мониторинг
```bash
# Просмотр всех логов с цветной подсветкой
./scripts/monitor-logs.sh

# Просмотр только сообщений Telegram
./watch-messages.sh

# Быстрая проверка статуса
./check-status.sh
```

### 2. Детальный мониторинг
```bash
# Полный лог в реальном времени
tail -f agent.log

# Только успешные операции
tail -f agent.log | grep "✅"

# Только ошибки
tail -f agent.log | grep "❌"

# Только входящие сообщения
tail -f agent.log | grep "🔔"

# Только сообщения о группе 2298297094
tail -f agent.log | grep "2298297094"

# Только ИИ-операции
tail -f agent.log | grep -E "🤖|📤|💬.*VIBEE"

# Фильтрация по времени (последние 50 строк)
tail -50 agent.log

# Поиск по ключевому слову
grep -i "фильтр\|мониторинг\| группа" agent.log
```

### 3. Комплексный мониторинг
```bash
# Демонстрация мониторинга с симуляцией
./scripts/demo-monitoring.sh

# Мониторинг с фильтрацией эмодзи
tail -f agent.log | grep --line-buffered "🔔\|📝\|⚠️\|📨\|🎯"
```

---

## 🎯 ПРАВИЛА ЛОГИРОВАНИЯ

### Для разработчиков (в коде)

#### 1. Всегда используйте цветные утилиты
```typescript
// ❌ НЕПРАВИЛЬНО
console.log('Connected to Telegram')

// ✅ ПРАВИЛЬНО
console.log(`${colorize.success('✅ Connected to Telegram!')}`)
```

#### 2. Структура лог-сообщения
```typescript
// ✅ ПРАВИЛЬНО
console.log(`${colorize.time(`[${timestamp}]`)} 🔔 ${colorize.chat(chatTitle)} » ${colorize.user(fromFirstName)}: ${colorize.message(text.substring(0, 100))}`)

// Структура: [ВРЕМЯ] ЭМОДЗИ ГРУППА » ПОЛЬЗОВАТЕЛЬ: ТЕКСТ
```

#### 3. Уровни логирования
```typescript
// DEBUG (серый) - отладочная информация
console.log(`${colors.gray}Debug info: ${debugData}${colors.reset}`)

// INFO (синий) - общая информация
console.log(`${colors.blue}Info: Starting service${colors.reset}`)

// WARN (желтый) - предупреждения
console.log(`${colors.yellow}⚠️ Warning: Low memory${colors.reset}`)

// ERROR (красный) - ошибки
console.log(`${colors.red}❌ Error: Failed to connect${colors.reset}`)
```

#### 4. Контекстное логирование
```typescript
// Всегда включайте контекст
console.log(`🎯 [TelegramService] Processing message from chat:`, {
  chatId: finalChatId,
  chatTitle,
  sender: senderId,
  messageId: message.id
})
```

### Для DevOps (команды)

#### 1. Ежедневный мониторинг
```bash
# Утренняя проверка статуса
./check-status.sh

# Просмотр последних 100 строк
tail -100 agent.log

# Проверка ошибок за последний час
grep "$(date '+%Y-%m-%d %H'):" agent.log | grep "❌"
```

#### 2. Диагностика проблем
```bash
# Найти все ошибки за сегодня
grep "$(date '+%Y-%m-%d')" agent.log | grep -i "error\|fail\|❌"

# Найти проблемы с подключением
grep -i "telegram\|mtproto\|connected" agent.log | tail -20

# Проверить производительность
grep -E "duration|time|ms" agent.log | tail -10
```

#### 3. Мониторинг в продакшене
```bash
# Непрерывный мониторинг с алертами
tail -f agent.log | while read line; do
  if [[ "$line" == *"❌"* ]]; then
    echo "🚨 ERROR DETECTED: $line"
    # Отправить уведомление в Slack/Discord
  fi
done

# Сохранение логов с ротацией
logrotate -f /etc/logrotate.d/vibee-agent
```

---

## 🔍 ФИЛЬТРАЦИЯ И ПОИСК

### Полезные grep-паттерны
```bash
# Подключение к Telegram
grep -E "(Connected to Telegram|User ID:|LAYER)" agent.log

# Групповая фильтрация
grep "2298297094" agent.log  # Только разрешенная группа
grep -v "ИГНОРИРУЕМ" agent.log  # Исключить игнорируемые

# Сообщения пользователей
grep -E "📨|💬.*»" agent.log

# ИИ-ответы
grep -E "🤖|Generating reply|Sending reply" agent.log

# Ошибки и предупреждения
grep -E "❌|⚠️|Error|Warning" agent.log
```

### Поиск по времени
```bash
# За последний час
grep "$(date -d '1 hour ago' '+%Y-%m-%d %H'):" agent.log

# За сегодня
grep "$(date '+%Y-%m-%d')" agent.log

# Диапазон дат
sed -n '/2025-11-22 10:/, /2025-11-22 12:/p' agent.log
```

---

## 📊 АНАЛИТИКА И МЕТРИКИ

### Статистика сообщений
```bash
# Подсчет сообщений по эмодзи
echo "📨 Всего сообщений: $(grep -c '📨' agent.log)"
echo "🔔 Входящих: $(grep -c '🔔' agent.log)"
echo "📤 Исходящих: $(grep -c '📤' agent.log)"
echo "❌ Ошибок: $(grep -c '❌' agent.log)"
echo "✅ Успехов: $(grep -c '✅' agent.log)"
```

### Производительность
```bash
# Время обработки сообщений
grep "duration\|time" agent.log | awk '{sum+=$NF; count++} END {print "Avg time: " sum/count "ms"}'

# Пиковые нагрузки
grep "message.*received" agent.log | wc -l  # Сообщений/час
```

---

## 🚨 АЛЕРТЫ И УВЕДОМЛЕНИЯ

### Критические события
```bash
# Остановка мониторинга
grep -i "stop\|disconnect\|offline" agent.log | tail -5

# Ошибки аутентификации
grep -i "auth\|login\|session" agent.log | grep -i "fail\|error"

# Проблемы с группами
grep -i "group\|chat.*forbidden\|permission" agent.log
```

### Автоматические алерты
```bash
# Скрипт алертов (alerter.sh)
#!/bin/bash
ERRORS=$(tail -100 agent.log | grep -c "❌")
if [ $ERRORS -gt 5 ]; then
  echo "🚨 High error rate detected: $ERRORS errors in last 100 lines"
  # send Slack notification
fi
```

---

## 📋 ЧЕКЛИСТ ДЛЯ РАЗРАБОТЧИКОВ

### Перед коммитом
- [ ] Все console.log используют colorize утилиты
- [ ] Логи содержат эмодзи-префиксы для быстрой фильтрации
- [ ] Нет чувствительных данных в логах (пароли, токены)
- [ ] Уровни логирования соответствуют важности события

### При отладке
- [ ] Использую правильный лог-файл (agent.log)
- [ ] Применяю цветовые фильтры для быстрого поиска
- [ ] Сохраняю логи проблемных сессий
- [ ] Документирую найденные проблемы

### При деплое
- [ ] Настроена ротация логов
- [ ] Настроены алерты на критические ошибки
- [ ] Логи архивируются для анализа трендов
- [ ] Настроен мониторинг в реальном времени

---

## 🆘 TROUBLESHOOTING

### Агент не отвечает
```bash
# 1. Проверить статус
./check-status.sh

# 2. Проверить подключение
tail -20 agent.log | grep -E "Connected|Error"

# 3. Проверить фильтрацию
grep "ИГНОРИРУЕМ" agent.log | tail -5
```

### Много ошибок
```bash
# Найти все ошибки за последние 10 минут
grep "$(date -d '10 minutes ago' '+%Y-%m-%d %H:%M')" agent.log | grep "❌"

# Проанализировать типы ошибок
grep "❌" agent.log | cut -d: -f4- | sort | uniq -c | sort -rn
```

### Нет сообщений из Telegram
```bash
# Проверить подключение к MTProto
tail -50 agent.log | grep -E "MTProto|Telegram|Connected"

# Проверить сессию
grep "session\|auth" agent.log | tail -5

# Проверить API ключи
grep "API.*ID\|API.*Hash" agent.log | tail -3
```

---

## 📚 РЕСУРСЫ

### Документация
- **CLAUDE.md** - Руководство для разработчиков
- **LOGS-MANAGEMENT.md** - Этот файл (ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ)
- **Rainbow Bridge Testing** - Автономное тестирование

### Скрипты
- `./scripts/monitor-logs.sh` - Цветной мониторинг логов
- `./scripts/demo-monitoring.sh` - Демонстрация мониторинга
- `./check-status.sh` - Быстрая проверка статуса
- `./watch-messages.sh` - Просмотр сообщений в реальном времени

### Команды
```bash
# Быстрый старт мониторинга
./scripts/monitor-logs.sh

# Проверка здоровья системы
./check-status.sh

# Демонстрация возможностей
./scripts/demo-monitoring.sh
```

---

## 🎉 ЗАКЛЮЧЕНИЕ

**ЛОГИРОВАНИЕ - ЭТО НЕ ПРОСТО ВЫВОД ТЕКСТА, ЭТО СИСТЕМА!**

Следуя этому руководству, вы получаете:
- ✅ **Централизованный контроль** - все логи в одном месте
- ✅ **Быструю диагностику** - цветовая инфографика
- ✅ **Профессиональный мониторинг** - готовые скрипты
- ✅ **Масштабируемость** - легко добавлять новые источники логов

**Помните**: Хорошие логи спасают время при отладке!

---

**📅 Создано**: 2025-11-22
**🔄 Обновлено**: 2025-11-22
**👨‍💻 Автор**: Claude Code
**🎯 Версия**: 1.0
