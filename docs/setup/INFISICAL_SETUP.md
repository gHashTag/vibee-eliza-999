# 🔐 Настройка Infisical для VIBEE

## 🚨 ПРАВИЛО ДЛЯ ВСЕХ АГЕНТОВ - ЧИТАЙ ОБЯЗАТЕЛЬНО!

**🔥 ВСЕГДА ЗАПУСКАЙ: `npm run dev` или `npm run dev:hot`**
**❌ НИКОГДА НЕ ЗАПУСКАЙ: `elizaos dev` - ЭТО ЛОМАЕТ INFISICAL!**

---

## ❗️ ПОДРОБНОЕ ОБЪЯСНЕНИЕ

**ВСЕГДА используйте эти команды для запуска VIBEE агента:**

### ✅ Правильный запуск (с Infisical)
```bash
# ОСНОВНАЯ КОМАНДА - используйте ВСЕГДА!
npm run dev

# или с hot reload
npm run dev:hot
```

### ❌ НЕПРАВИЛЬНЫЙ запуск (без Infisical)
```bash
# НЕ ИСПОЛЬЗУЙТЕ - не загружает секреты!
npx elizaos dev
# или
elizaos dev
```

## Почему это важно?

При запуске `npm run dev` автоматически:
1. ✅ Загружаются секреты из `.infisical.env` (OPENROUTER_API_KEY, TELEGRAM_BOT_TOKEN и 50+ других)
2. ✅ Подключается OpenRouter для умных ответов
3. ✅ Агент отвечает ИИ-ответками, а не шаблонами

При запуске `elizaos dev` напрямую:
1. ❌ Секреты НЕ загружаются
2. ❌ Нет LLM ключей (OpenRouter, OpenAI, Anthropic)
3. ❌ Агент отвечает только шаблонными фразами

## 📋 Проверка подключения Infisical

После запуска проверьте логи:
```bash
# Должно быть:
🔑 Загружено секретов из .infisical.env
Final plugins being loaded: {plugins: [ "openrouter", ... ]}

# И статус:
bash scripts/log-viewer.sh status
```

## 🐛 Если Infisical не работает

1. **Проверьте файл `.infisical.env`:**
   ```bash
   cat .infisical.env | grep OPENROUTER_API_KEY
   ```
   Должно показать ключ: `sk-or-v1-...`

2. **Проверьте скрипт запуска:**
   ```bash
   cat package.json | grep -A3 '"dev"'
   ```
   Должно быть: `"dev": "bash scripts/dev-with-infisical.sh && elizaos dev"`

3. **Перезапустите агента:**
   ```bash
   pkill -f "elizaos dev"
   npm run dev
   ```

## 📦 Структура секретов

- `.env.dev` - только настройки Infisical (5 переменных)
- `.infisical.env` - все API ключи (загружается из Infisical Cloud)
- Все 50+ секретов управляются в Infisical Cloud

## 🚀 Готовые команды для агентов

```bash
# Запуск VIBEE с Infisical
npm run dev

# Проверка статуса
bash scripts/log-viewer.sh status

# Мониторинг логов
bash scripts/log-viewer.sh live

# Проверка ошибок
bash scripts/log-viewer.sh errors
```

---

**💡 Запомните: VIBEE = npm run dev (с Infisical), а НЕ elizaos dev (без секретов)!**
