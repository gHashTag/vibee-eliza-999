# 🧪 Руководство по тестированию системы загрузки файлов

## ⚠️ ВАЖНО: Запуск из корневой директории

Агент ДОЛЖЕН запускаться из корневой директории проекта `/Users/playra/vibee-agent/`, НЕ из поддиректорий!

### Правильный запуск:
```bash
cd /Users/playra/vibee-agent/
npm run dev
```

### НЕПРАВИЛЬНО (вызовет ошибку БД):
```bash
cd /Users/playra/vibee-agent/plugin-vibe-face-avatar/
npm run dev  # ❌ ОШИБКА!
```

## 📋 Шаги тестирования

### 1. Подготовка Supabase
```bash
# Создать проект в Supabase
# Создать bucket 'instagram-uploads'
# Настроить RLS политики

# Получить ключи:
# - Project URL
# - anon key (публичный)
# - service_role key (приватный)
```

### 2. Настройка переменных

#### В Infisical (Backend):
```bash
infisical login
infisical secrets set SUPABASE_URL=https://your-project.supabase.co --env=dev
infisical secrets set SUPABASE_SERVICE_ROLE_KEY=your-service-role-key --env=dev
```

#### В .env (Frontend):
```bash
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

### 3. Запуск агента
```bash
cd /Users/playra/vibee-agent/
npm run dev
```

### 4. Проверка логов

Должны появиться сообщения:
```
🐝 Инициализация Instagram плагина...
✅ Storage Service инициализирован (Supabase Storage с автоочисткой)
✅ Instagram токены настроены
```

### 5. Тестирование загрузки

1. Открыть веб-интерфейс агента
2. Перейти на вкладку "File Upload"
3. Перетащить изображение
4. Проверить появление URL

### 6. Тестирование Instagram

Отправить боту:
```
Опубликуй пост в Instagram <URL> с подписью тест
```

## 🐛 Устранение неполадок

### Ошибка: "Database migration failed"
**Причина**: Агент запущен из поддиректории
**Решение**: `cd /Users/playra/vibee-agent/ && npm run dev`

### Ошибка: "Storage Service не инициализирован"
**Причина**: Нет SUPABASE_URL в Infisical
**Решение**: Добавить переменные через Infisical CLI

### Ошибка: "Failed to fetch" (фронтенд)
**Причина**: Неверные VITE_ переменные в .env
**Решение**: Проверить VITE_SUPABASE_URL и VITE_SUPABASE_ANON_KEY

### Bucket не найден
**Причина**: Bucket 'instagram-uploads' не создан
**Решение**: Создать bucket в Supabase Dashboard

## 📁 Структура файлов

```
/Users/playra/vibee-agent/
├── src/frontend/
│   ├── supabase.ts                    ← Supabase клиент (фронтенд)
│   ├── components/FileUpload.tsx      ← React компонент
│   └── index.tsx                      ← Панели интерфейса
├── plugin-vibe-instagram/
│   ├── src/services/storageService.ts ← Storage сервис (бэкенд)
│   └── src/index.ts                   ← Экспорт плагина
├── .env                               ← VITE_ переменные
└── TESTING_GUIDE.md                   ← Этот файл
```

## 📊 Ожидаемые результаты

### ✅ Успешная инициализация:
```
[INIT] Loading character...
[INIT] Character loaded: AssistantAgent
[INIT] Loading plugins...
[INIT] Plugin loaded: instagram
🐝 Инициализация Instagram плагина...
✅ Storage Service инициализирован (Supabase Storage с автоочисткой)
✅ Instagram токены настроены
[INIT] Database migrations completed successfully
[INIT] Agent Server running on http://localhost:3000
```

### ✅ Успешная загрузка файла:
```
✅ [Supabase] Файл загружен: https://.../instagram-uploads/1234567890-abc.jpg
✅ [FileUpload] Файл загружен: https://.../instagram-uploads/1234567890-abc.jpg
```

## 🔑 Ключевые команды

```bash
# Проверить секреты в Infisical
infisical secrets --env=dev | grep SUPABASE

# Запустить агента
cd /Users/playra/vibee-agent/ && npm run dev

# Посмотреть логи
tail -f agent.log | grep -i "instagram\|Storage"

# Проверить типы
npm run type-check

# Проверить форматирование
npm run format:check
```

## 📝 Чек-лист тестирования

- [ ] Агент запущен из `/Users/playra/vibee-agent/`
- [ ] Supabase переменные добавлены в Infisical
- [ ] VITE_ переменные добавлены в `.env`
- [ ] Bucket 'instagram-uploads' создан
- [ ] В логах: "Storage Service инициализирован"
- [ ] Веб-интерфейс: вкладка "File Upload" работает
- [ ] Загрузка файла: появляется URL
- [ ] Instagram: команда обрабатывается

## 🚀 Готово к продакшену!

После успешного тестирования всех пунктов система готова к использованию.
