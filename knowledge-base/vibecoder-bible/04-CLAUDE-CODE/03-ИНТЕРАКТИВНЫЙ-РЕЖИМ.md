# 💬 ИНТЕРАКТИВНЫЙ РЕЖИМ

> **"Разговор с Claude Code — это медитация через диалог с AI."**

---

## 🎯 Что вы узнаете

- ✅ Как запускать и работать в интерактивной сессии
- ✅ Основные команды и shortcuts
- ✅ Checkpoint система для отката изменений
- ✅ Управление контекстом проекта
- ✅ Best practices для эффективной работы

**Время:** 15-20 минут чтения + практика

---

## 🚀 Запуск интерактивного режима

### **Базовый запуск**

```bash
# Перейдите в ваш проект
cd ~/projects/my-telegram-bot

# Запустите Claude Code
claude

# Вы увидите приветствие:
╭─────────────────────────────────────────╮
│  Claude Code 2.x.x                     │
│  Context: my-telegram-bot/              │
│  Ready to help! Type /help for commands │
╰─────────────────────────────────────────╯

>
```

### **Запуск с конкретной задачей**

```bash
# Сразу задать вопрос
claude "Создай структуру для Telegram бота"

# Или с файлом
claude "Объясни что делает этот файл" src/index.js
```

---

## 💬 Как общаться с Claude

### **Принципы эффективного общения**

**1. Будьте конкретны**

❌ **Плохо:** "Сделай что-нибудь с ботом"

✅ **Хорошо:** "Добавь в бота команду /help которая показывает список команд"

**2. Описывайте контекст**

❌ **Плохо:** "Исправь ошибку"

✅ **Хорошо:** "В файле bot.js функция sendMessage выдаёт TypeError. Исправь проверку типов"

**3. Разбивайте большие задачи**

❌ **Плохо:** "Создай полностью готовый интернет-магазин"

✅ **Хорошо:**
```
1. "Создай структуру проекта для интернет-магазина"
2. "Добавь модель Product с полями: name, price, description"
3. "Создай API эндпоинт для получения списка товаров"
```

**4. Используйте естественный язык**

✅ Говорите как с коллегой, а не как с роботом:
- "Можешь помочь оптимизировать этот запрос к БД?"
- "Мне кажется здесь есть утечка памяти, посмотри пожалуйста"
- "Добавь комментарии к этой функции, она сложная"

---

## ⌨️ Основные команды

### **Встроенные slash команды**

```bash
/help           # Показать список команд
/clear          # Очистить контекст разговора
/exit           # Выйти из Claude Code (или Ctrl+D)
/review         # Ревью последних изменений
/rewind         # Откатить последние изменения
/agents         # Управление субагентами
/history        # Показать историю команд
```

### **Навигация и контроль**

```bash
# Прервать выполнение
Ctrl+C          # Остановить текущую операцию

# Откат изменений (Checkpoint система)
ESC ESC         # Откатить последнее изменение
/rewind         # Откатить через команду

# Очистить экран
Ctrl+L          # Очистить терминал

# Выход
Ctrl+D          # Выйти из сессии
/exit           # Альтернативный выход
```

---

## 🔄 Checkpoint система

Claude Code автоматически сохраняет состояние перед каждым изменением.

### **Автоматические checkpoint'ы**

```
> Добавь валидацию email в функцию register

✓ Checkpoint created: before editing src/auth.js
✓ File modified: src/auth.js
✓ Changes applied successfully
```

### **Откат изменений**

**Метод 1: Нажать ESC ESC**
```
> Добавь новую функцию...
[Claude делает изменения]

# Передумали? Нажмите ESC два раза
ESC ESC

✓ Reverted to previous checkpoint
✓ Changes undone: src/auth.js
```

**Метод 2: Команда /rewind**
```
> /rewind

Available checkpoints:
1. Before editing src/auth.js (5 min ago)
2. Before creating src/utils.js (15 min ago)
3. Before deleting old-code.js (30 min ago)

Select checkpoint to restore [1-3]: 2

✓ Restored to checkpoint: Before creating src/utils.js
```

### **Просмотр checkpoint'ов**

```bash
> /checkpoints

📋 Recent Checkpoints:
┌────┬──────────────────────┬─────────────┬──────────────────┐
│ ID │ Description          │ Time        │ Files Changed    │
├────┼──────────────────────┼─────────────┼──────────────────┤
│ 1  │ Before editing auth  │ 5 min ago   │ src/auth.js      │
│ 2  │ Before creating util │ 15 min ago  │ src/utils.js     │
│ 3  │ Before git commit    │ 30 min ago  │ 5 files          │
└────┴──────────────────────┴─────────────┴──────────────────┘
```

`★ Insight ─────────────────────────────────────`
**Безопасность через checkpoint'ы**: Система checkpoint'ов в Claude Code реализует философию "экспериментируй без страха". Вы можете пробовать смелые решения, зная что всегда можно вернуться назад. Это как сохранения в видеоиграх — смерть не страшна, когда есть save point.
`─────────────────────────────────────────────────`

---

## 🎨 Работа с контекстом

### **Что Claude видит в контексте**

```
> Что ты знаешь об этом проекте?

Analyzing your project...

📁 Project Structure:
- Language: TypeScript (detected from package.json)
- Framework: Express.js for API
- Database: PostgreSQL with Prisma ORM
- Total files: 47 (.ts, .js, .json)
- Dependencies: 23 packages
- Test coverage: ~65% (from jest.config.js)

📦 Main Modules:
- src/api/ - REST API endpoints
- src/db/ - Database models and migrations
- src/auth/ - Authentication logic
- tests/ - Unit and integration tests

🔍 Recent Activity:
- Last git commit: 2 hours ago "Add user registration"
- Modified files: src/auth/register.ts, src/db/models/User.ts
```

### **Добавление файлов в контекст**

```bash
# Явно указать файл
> Посмотри на src/auth.js и скажи есть ли проблемы

# Добавить несколько файлов
> Сравни src/api/v1/users.js и src/api/v2/users.js

# Работа с папкой
> Проанализируй все файлы в папке tests/
```

### **Ограничение контекста**

```bash
# Очистить контекст (забыть предыдущий разговор)
> /clear

Контекст очищен. Начнём с чистого листа!

# Сфокусироваться только на одном модуле
> /focus src/auth/
Теперь я буду работать только с файлами в src/auth/
```

---

## 🔥 Типичные сценарии использования

### **Сценарий 1: Создание нового функционала**

```
> Нужно добавить в бота функцию для экспорта данных в CSV

Claude: Понял. Создам функцию exportToCSV. Какие данные нужно экспортировать?

> Список пользователей с их подписками

Claude: ✓ Created src/export.js with exportUsersToCSV()
        ✓ Added /export command to bot handlers
        ✓ Installed csv-writer package
        ✓ Added tests to tests/export.test.js

        Хотите протестировать команду?

> Да, запусти тесты

Claude: Running: npm test export.test.js
        ✓ All tests passed (4/4)
```

### **Сценарий 2: Отладка ошибки**

```
> Бот падает с ошибкой "Cannot read property 'id' of undefined"

Claude: Analyzing error...

        Нашёл проблему в src/handlers/message.js:42

        Переменная user может быть undefined если пользователь
        не зарегистрирован. Добавлю проверку:

        if (!user) {
          return await ctx.reply('Пожалуйста, сначала /start');
        }

        Применить исправление?

> Да

Claude: ✓ Fixed: Added user validation check
        ✓ Added similar checks in 3 other handlers
        ✓ Added error handling tests

        Проблема решена!
```

### **Сценарий 3: Рефакторинг**

```
> Функция processPayment слишком большая. Разбей её на части

Claude: Analyzing src/payment.js...

        Функция processPayment (87 строк) делает слишком много:
        - Валидация данных
        - Вызов платёжного API
        - Обновление базы данных
        - Отправка уведомлений

        Разобью на 4 функции:
        1. validatePaymentData()
        2. callPaymentGateway()
        3. updatePaymentRecord()
        4. sendPaymentNotification()

        Продолжить?

> Да, и добавь типы TypeScript

Claude: ✓ Refactored: Split processPayment into 4 functions
        ✓ Added TypeScript interfaces for Payment types
        ✓ Added JSDoc comments
        ✓ All tests still passing

        Code is now more maintainable!
```

---

## ⚡ Горячие клавиши и shortcuts

```bash
# Навигация
↑/↓              # История команд
Ctrl+R           # Поиск в истории команд
Tab              # Автодополнение путей

# Редактирование
Ctrl+A           # Начало строки
Ctrl+E           # Конец строки
Ctrl+U           # Удалить строку
Ctrl+K           # Удалить от курсора до конца

# Контроль
Ctrl+C           # Прервать выполнение
Ctrl+D           # Выход
Ctrl+L           # Очистить экран
ESC ESC          # Откат изменений

# Специальные
Ctrl+Z           # Приостановить (resume через fg)
```

---

## 💡 Best Practices

### **1. Используйте checkpoint'ы осознанно**

```bash
# Создайте checkpoint перед рискованными изменениями
> /checkpoint "Before major refactoring"

# Экспериментируйте свободно
> Попробуй переписать это на функциональный стиль

# Не понравилось? Откатывайтесь
> ESC ESC
```

### **2. Очищайте контекст когда нужно**

```bash
# После завершения большой задачи
> /clear

# При переключении между модулями
> /focus src/api/
```

### **3. Сохраняйте промежуточные результаты**

```bash
# После значимых изменений делайте коммит
> Сделай git commit с описанием изменений

# Checkpoint автоматический, но коммит — явное сохранение
```

### **4. Разбивайте сложные задачи**

```bash
# Вместо одного большого запроса
❌ "Создай полный бэкенд с авторизацией и платежами"

# Делайте пошагово
✅ "Создай структуру Express проекта"
✅ "Добавь JWT авторизацию"
✅ "Интегрируй Stripe для платежей"
```

---

## 🎓 Практические упражнения

### **Упражнение 1: Первый диалог (5 мин)**

1. Запустите `claude` в вашем проекте
2. Спросите: "Расскажи о структуре этого проекта"
3. Попросите: "Создай файл CONTRIBUTING.md с правилами контрибуции"
4. Откатите изменение через ESC ESC
5. Выйдите через /exit

### **Упражнение 2: Работа с checkpoint'ами (10 мин)**

1. Создайте новый файл: "Создай utils/math.js с функциями sum и multiply"
2. Попросите улучшить: "Добавь проверку типов и обработку ошибок"
3. Посмотрите checkpoint'ы: `/checkpoints`
4. Откатитесь к первой версии: ESC ESC
5. Сравните результаты

### **Упражнение 3: Рефакторинг (15 мин)**

1. Найдите большую функцию в своём проекте
2. Попросите: "Проанализируй функцию X и предложи улучшения"
3. Примените улучшения
4. Запустите тесты
5. Если всё ОК — сделайте git commit

---

## 🐛 Troubleshooting

### **Claude не видит мои файлы**

```bash
# Проверьте что вы в правильной папке
pwd

# Claude игнорирует node_modules, .git по умолчанию
# Если нужно посмотреть — укажите явно
> Посмотри на node_modules/my-package/index.js
```

### **Контекст переполнен**

```bash
# Очистите контекст
> /clear

# Или сфокусируйтесь на конкретной папке
> /focus src/api/
```

### **Claude предлагает устаревшие решения**

```bash
# Укажите требования явно
> Используй современный подход с async/await, а не callbacks

# Или укажите версию технологии
> Используй Node.js 20 и новейший синтаксис
```

---

## 🌟 Следующие шаги

Освоили интерактивный режим? Переходите к следующим темам:

1. **[[04-РАБОТА-С-ФАЙЛАМИ.md]]** — Детальная работа с файлами
2. **[[05-SLASH-КОМАНДЫ.md]]** — Создание собственных команд
3. **[[06-СУБАГЕНТЫ.md]]** — Специализированные AI-помощники

---

`★ Insight ─────────────────────────────────────`
**Интерактивность как медитация**: Диалог с Claude Code — это не просто Q&A. Это совместное исследование пространства решений. Вы предлагаете направление, Claude показывает варианты, вы выбираете путь. Это танец двух интеллектов — человеческого и искусственного.
`─────────────────────────────────────────────────`

---

## 📝 Проверь себя

### **Тест 1: Основы интерактивного режима**

**Вопрос 1:** Что произойдёт если в интерактивном режиме нажать ESC ESC?

<details>
<summary>Показать ответ</summary>

**Ответ:** Будет выполнен откат к предыдущему checkpoint'у (rewind). Claude Code автоматически сохраняет состояние перед каждым изменением, и ESC ESC возвращает проект к состоянию до последнего изменения.

**Альтернативный способ:** Команда `/rewind` с выбором конкретного checkpoint'а из списка.

</details>

**Вопрос 2:** Какая разница между командами `/clear` и `/exit`?

<details>
<summary>Показать ответ</summary>

**Ответ:**
- `/clear` — очищает контекст разговора (Claude "забывает" предыдущий диалог), но остаётся в интерактивной сессии. Полезно при переключении между разными задачами.
- `/exit` — полностью завершает интерактивную сессию Claude Code и возвращает в обычный терминал. То же самое делает `Ctrl+D`.

</details>

**Вопрос 3:** Как запустить Claude Code сразу с конкретным вопросом, не входя в интерактивный режим?

<details>
<summary>Показать ответ</summary>

**Ответ:**
```bash
# Базовый способ
claude "Создай структуру для REST API"

# С указанием файла
claude "Объясни что делает этот код" src/index.js

# Многострочный вопрос
claude "Проанализируй проект и:
1. Найди неиспользуемые зависимости
2. Предложи оптимизации
3. Проверь безопасность"
```

Claude выполнит задачу и вернёт вас в терминал без запуска интерактивной сессии.

</details>

**Вопрос 4:** Что Claude Code видит в контексте проекта автоматически при запуске?

<details>
<summary>Показать ответ</summary>

**Ответ:** При запуске Claude Code автоматически анализирует:

1. **Структуру проекта:**
   - Язык программирования (из `package.json`, файлов)
   - Фреймворки и библиотеки
   - Общее количество файлов

2. **Зависимости:**
   - `package.json` / `requirements.txt` / `Cargo.toml`
   - Установленные пакеты

3. **Git информация:**
   - Последние коммиты
   - Измененные файлы
   - Текущая ветка

4. **Конфигурация:**
   - Настройки линтеров, тестов
   - `.env.example` (но НЕ `.env` с секретами!)

**НЕ видит автоматически:** Содержимое файлов в `node_modules/`, `.git/`, файлы из `.gitignore`.

</details>

---

### **Тест 2: Команды и навигация**

**Вопрос 5:** Какие три способа прервать текущую операцию Claude Code?

<details>
<summary>Показать ответ</summary>

**Ответ:**

1. **`Ctrl+C`** — остановить текущее выполнение (например, если Claude генерирует слишком много кода)
2. **ESC ESC** — откатить уже выполненное изменение к checkpoint'у
3. **`Ctrl+Z`** — приостановить процесс (resume через команду `fg` в терминале)

**Дополнительно:** `/exit` или `Ctrl+D` — полный выход из сессии.

</details>

**Вопрос 6:** Как посмотреть список всех доступных checkpoint'ов?

<details>
<summary>Показать ответ</summary>

**Ответ:**
```bash
> /checkpoints

# Отобразится таблица:
📋 Recent Checkpoints:
┌────┬──────────────────────┬─────────────┬──────────────────┐
│ ID │ Description          │ Time        │ Files Changed    │
├────┼──────────────────────┼─────────────┼──────────────────┤
│ 1  │ Before editing auth  │ 5 min ago   │ src/auth.js      │
│ 2  │ Before creating util │ 15 min ago  │ src/utils.js     │
│ 3  │ Before git commit    │ 30 min ago  │ 5 files          │
└────┴──────────────────────┴─────────────┴──────────────────┘
```

Для восстановления конкретного checkpoint'а: `/rewind` + выбор ID.

</details>

**Вопрос 7:** Как ограничить контекст Claude только одной конкретной папкой?

<details>
<summary>Показать ответ</summary>

**Ответ:**
```bash
> /focus src/auth/

# Claude ответит:
Теперь я буду работать только с файлами в src/auth/
```

**Польза:**
- Ускоряет работу (меньше контекста для анализа)
- Предотвращает случайное изменение файлов вне фокуса
- Полезно для больших проектов

**Сброс фокуса:** `/focus .` или `/clear`

</details>

---

### **Тест 3: Best Practices**

**Вопрос 8:** Почему важно делать git commit после значимых изменений, если есть checkpoint система?

<details>
<summary>Показать ответ</summary>

**Ответ:**

**Checkpoint'ы — временные:**
- Хранятся только в текущей сессии Claude Code
- Автоматически создаются перед изменениями
- Могут быть перезаписаны новыми checkpoint'ами
- НЕ сохраняются между запусками Claude

**Git commit — постоянное сохранение:**
- Сохраняется в истории проекта навсегда
- Доступен в любое время (не только в Claude)
- Синхронизируется с удалённым репозиторием
- Содержит описание изменений

**Правило:** Checkpoint для экспериментов, git commit для готовых решений.

```bash
# Workflow
1. Эксперимент → автоматический checkpoint
2. Понравилось → git commit
3. Не понравилось → ESC ESC (rewind)
```

</details>

**Вопрос 9:** Как правильно формулировать большие задачи для Claude?

<details>
<summary>Показать ответ</summary>

**Ответ:**

❌ **НЕПРАВИЛЬНО — одним большим запросом:**
```
Создай полностью готовый интернет-магазин с корзиной,
оплатой, авторизацией, админкой и мобильным приложением
```

✅ **ПРАВИЛЬНО — разбить на шаги:**
```
Шаг 1: "Создай структуру проекта для интернет-магазина"
Шаг 2: "Добавь модель Product с полями name, price, description"
Шаг 3: "Создай API эндпоинт GET /api/products"
Шаг 4: "Добавь корзину с функциями add/remove/clear"
Шаг 5: "Интегрируй Stripe для оплаты"
...
```

**Преимущества разбиения:**
- Каждый шаг можно проверить отдельно
- Легко откатить неудачный шаг
- Claude лучше понимает контекст
- Вы контролируете процесс на каждом этапе

</details>

**Вопрос 10:** В каких случаях стоит использовать команду `/clear`?

<details>
<summary>Показать ответ</summary>

**Ответ:**

**Используйте `/clear` когда:**

1. **Переключаетесь на другую задачу:**
   ```
   > Закончил работу с авторизацией
   > /clear
   > Теперь буду работать с платёжной системой
   ```

2. **Контекст переполнен:**
   ```
   # После долгого диалога Claude может "забыть" начало
   > /clear
   # Начинаете с чистого листа
   ```

3. **Claude даёт неожиданные ответы:**
   ```
   # Если Claude предлагает решения не в тему
   > /clear
   # Переформулируйте задачу заново
   ```

4. **После завершения большого рефакторинга:**
   ```
   > Рефакторинг завершён, сделан git commit
   > /clear
   # Готовы к новой задаче
   ```

**НЕ используйте `/clear`:**
- В середине сложной задачи (потеряете контекст)
- Если нужно сослаться на предыдущий диалог

</details>

---

## 🏆 Домашнее задание

### **Уровень 1: Базовое (15-25 минут)**
**Тема:** Освоение интерактивной сессии и основных команд

**Задание:**
1. Запустите Claude Code в любом вашем проекте
2. Выполните следующие команды последовательно:
   - `/help` — изучите список команд
   - Спросите: "Расскажи о структуре проекта"
   - Попросите: "Создай файл TODO.md со списком задач для проекта"
   - Посмотрите checkpoint'ы: `/checkpoints`
   - Откатите изменение: ESC ESC
   - Попросите снова создать TODO.md с другой структурой
   - Если устраивает — сделайте git commit через Claude
   - Очистите контекст: `/clear`
   - Выйдите: `/exit`

**Критерии выполнения:**
- ✅ Понимаете разницу между checkpoint и git commit
- ✅ Умеете откатывать изменения
- ✅ Знаете когда использовать /clear

---

### **Уровень 2: Продвинутое (30-45 минут)**
**Тема:** Мультизадачность в CLI — работа с несколькими окнами терминала

**Задание:**
Организуйте эффективный workflow с Claude Code используя несколько терминальных окон.

**Часть 1: Настройка окружения**
1. Установите tmux (если ещё не установлен):
   ```bash
   # macOS
   brew install tmux

   # Ubuntu/Debian
   sudo apt install tmux
   ```

2. Создайте tmux сессию с панелями:
   ```bash
   tmux new -s claude-workflow

   # Разделите окно:
   Ctrl+B "    # горизонтальное разделение
   Ctrl+B %    # вертикальное разделение
   Ctrl+B o    # переключение между панелями
   ```

**Часть 2: Организация workspace**
Создайте 4 панели:
- **Панель 1:** Claude Code интерактивный режим
- **Панель 2:** Мониторинг логов (`tail -f app.log` или `npm run dev`)
- **Панель 3:** Git операции (`git status`, `git diff`)
- **Панель 4:** Запуск тестов (`npm test -- --watch`)

**Часть 3: Практическая задача**
Используя этот setup, выполните:

1. В панели Claude попросите создать новый API эндпоинт
2. В панели с логами наблюдайте за изменениями
3. В панели Git проверьте какие файлы изменились
4. В панели с тестами убедитесь что тесты проходят
5. Если тесты упали — попросите Claude исправить
6. Сделайте git commit через панель Git

**Критерии выполнения:**
- ✅ Настроен tmux с 4 панелями
- ✅ Умеете переключаться между панелями
- ✅ Синхронно работаете с Claude и другими инструментами
- ✅ Workflow ускорился минимум на 30%

**Бонус:**
- Сохраните tmux конфигурацию в `~/.tmux.conf`
- Создайте alias для быстрого запуска: `alias cw='tmux new -s claude-workflow'`

---

### **Уровень 3: Проектное (50-60+ минут)**
**Тема:** Создание персонального CLI workflow с алиасами и автоматизацией

**Задание:**
Создайте персональную систему для максимально эффективной работы с Claude Code.

**Шаг 1: Анализ текущего workflow (10 мин)**
Ответьте на вопросы:
1. Какие команды Claude Code вы используете чаще всего?
2. Какие задачи повторяются ежедневно?
3. Сколько времени тратите на переключение между инструментами?
4. Какие ручные операции можно автоматизировать?

**Шаг 2: Создание custom shortcuts (15 мин)**

Создайте файл `~/.claude-aliases` со своими командами:

```bash
# ~/.claude-aliases

# Быстрый запуск Claude в проекте
alias c='claude'
alias ci='claude --interactive'
alias cq='claude'  # quick question

# Работа с checkpoint'ами
alias crw='claude /rewind'
alias cck='claude /checkpoints'

# Комплексные workflow
alias cdev='tmux new -s dev -d "claude" \; split-window -h "npm run dev" \; split-window -v "npm test -- --watch"'

# Быстрые вопросы
alias cfix='claude "Проанализируй последний git diff и найди потенциальные проблемы"'
alias ctest='claude "Создай тесты для последнего изменённого файла"'
alias cdoc='claude "Добавь JSDoc комментарии к функциям в последнем изменённом файле"'

# Git + Claude интеграция
alias cgc='claude "Проанализируй git diff и предложи commit message" && git add . && git commit'
```

Добавьте в `~/.zshrc` или `~/.bashrc`:
```bash
[ -f ~/.claude-aliases ] && source ~/.claude-aliases
```

**Шаг 3: Автоматизация с tmuxinator (15 мин)**

Установите tmuxinator:
```bash
gem install tmuxinator
```

Создайте конфиг `~/.config/tmuxinator/claude.yml`:

```yaml
name: claude-workspace
root: ~/projects/<your-project>

windows:
  - main:
      layout: main-vertical
      panes:
        - claude
        - npm run dev
  - code:
      layout: tiled
      panes:
        - vim  # или code .
        - git status
  - tests:
      - npm test -- --watch
  - logs:
      - tail -f app.log
```

Запуск: `tmuxinator start claude-workspace`

**Шаг 4: Pre-commit hooks с Claude (10 мин)**

Создайте `.git/hooks/pre-commit`:
```bash
#!/bin/bash

echo "🤖 Claude Code проверяет изменения..."

# Попросить Claude проверить код перед коммитом
claude "Проанализируй git diff --staged и найди:
1. Потенциальные баги
2. Проблемы с производительностью
3. Нарушения best practices
4. Закомментированный код

Если всё ок — выведи ✅ PASSED
Если есть проблемы — выведи ❌ и список проблем"

# Если Claude нашёл проблемы — спросить подтверждение
read -p "Продолжить коммит? (y/n) " -n 1 -r
echo
if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    exit 1
fi
```

**Шаг 5: Создание dashboard команды (10 мин)**

Создайте скрипт `~/bin/claude-dashboard`:

```bash
#!/bin/bash

clear
echo "╔════════════════════════════════════╗"
echo "║   CLAUDE CODE WORKFLOW DASHBOARD   ║"
echo "╚════════════════════════════════════╝"
echo ""
echo "📊 Статистика за сегодня:"
echo "   Проектов обработано: $(grep -c 'claude' ~/.claude_history)"
echo "   Checkpoint'ов создано: $(find .claude/checkpoints -type f -mtime -1 | wc -l)"
echo ""
echo "🚀 Быстрые команды:"
echo "   c          - Запустить Claude"
echo "   cdev       - Запустить dev workspace"
echo "   cgc        - Smart git commit"
echo "   cfix       - Исправить последние изменения"
echo ""
echo "📁 Текущий проект:"
pwd
git status -sb
echo ""
echo "💡 Последние checkpoint'ы:"
ls -lt .claude/checkpoints 2>/dev/null | head -5
```

Сделайте исполняемым: `chmod +x ~/bin/claude-dashboard`

Добавьте в `~/.zshrc`: `alias cd='claude-dashboard'`

**Критерии выполнения:**
- ✅ Создано минимум 5 полезных алиасов
- ✅ Настроен tmuxinator workspace
- ✅ Добавлен pre-commit hook с Claude
- ✅ Работает dashboard команда
- ✅ Workflow ускорился минимум на 50%

**Бонус-задания:**
1. Интеграция с Alfred/Raycast для быстрого запуска Claude команд
2. Создание Vim/VSCode плагина для отправки кода в Claude
3. Настройка webhook для автоматического запуска Claude при push в GitHub
4. Создание Telegram бота для удалённой работы с Claude Code

---

## 🎯 Критерии оценки ДЗ

### Уровень 1 (Базовое):
- **Зачёт:** Выполнены все команды, понятна работа checkpoint системы
- **Отлично:** + написан конспект с наблюдениями и скриншоты

### Уровень 2 (Продвинутое):
- **Зачёт:** Настроен tmux с 4 панелями, workflow работает
- **Отлично:** + создан `.tmux.conf` с персональными настройками + алиасы

### Уровень 3 (Проектное):
- **Зачёт:** Работают алиасы и tmuxinator, измерено улучшение workflow
- **Отлично:** + выполнено минимум 1 бонус-задание + опубликован setup на GitHub

---

`★ Insight ─────────────────────────────────────`
**Персонализация workflow**: Лучшие инструменты — это те, которые адаптированы под ваш стиль работы. Потратьте время на настройку один раз, и вы будете экономить часы каждую неделю. Ваш workflow должен быть как любимая пара кроссовок — идеально подогнанная под вас.
`─────────────────────────────────────────────────`

---

**Навигация:**
← [Предыдущая статья: 02. Установка и первый запуск](02-УСТАНОВКА-И-ПЕРВЫЙ-ЗАПУСК.md)
→ [Следующая статья: 04. Работа с файлами](04-РАБОТА-С-ФАЙЛАМИ.md)

**Прогресс:** 3 из 11 статей раздела "Claude Code" | [Вернуться к содержанию](README.md)

*Создано с вниманием к деталям для вайбкодеров 💚*
