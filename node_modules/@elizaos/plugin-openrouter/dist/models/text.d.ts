import type { GenerateTextParams, IAgentRuntime } from "@elizaos/core";
import type { Tool, ToolChoice } from "ai";
import type { ToolResponse } from "../types";
/**
 * TEXT_SMALL model handler
 */
export declare function handleTextSmall(runtime: IAgentRuntime, params: GenerateTextParams & {
    tools?: Record<string, Tool>;
    toolChoice?: ToolChoice<Record<string, Tool>>;
}): Promise<string | ToolResponse>;
/**
 * TEXT_LARGE model handler
 */
export declare function handleTextLarge(runtime: IAgentRuntime, params: GenerateTextParams & {
    tools?: Record<string, Tool>;
    toolChoice?: ToolChoice<Record<string, Tool>>;
}): Promise<string | ToolResponse>;
