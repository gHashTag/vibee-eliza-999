# 🤖 Агенты VIBEE

> **Единое место хранения Character Files (.json) для тестирования и прототипирования**

## ⚠️ ВАЖНО

**Для production агентов используйте TypeScript агенты в проектах:**
- TypeScript агенты → `my-project/src/agents/` или `my-project/src/index.ts`
- JSON агенты (эта папка) → только для тестирования и прототипирования

## 📁 Структура

Все character files (.json) для тестирования должны храниться **ТОЛЬКО** в этой директории.

```
packages/agents/
├── vibeeAgent.json          # Главный агент VIBEE
├── kolsAgent.json           # Агент для мониторинга KOLs
├── neuroPhoto.json          # Агент для генерации изображений
├── instagramExpert.json     # Агент для Instagram автоматизации
└── README.md                # Этот файл
```

## ⚠️ ПРАВИЛА

### ✅ ОБЯЗАТЕЛЬНО:
- ✅ Все character files (.json) → `packages/agents/`
- ✅ Именование: `{agentName}Agent.json` (camelCase)
- ✅ При создании нового агента → сохранять в `packages/agents/`

### ❌ ЗАПРЕЩЕНО:
- ❌ Хранить агенты в `agents-json/` (устаревшая папка)
- ❌ Хранить агенты в корне проекта
- ❌ Дублировать агентов в разных местах
- ❌ Создавать агентов вне `packages/agents/`

## 🚀 Использование

### Запуск агента:

```bash
# Из этой директории
elizaos start --character packages/agents/vibeeAgent.json

# Или через agent команду (если сервер уже запущен)
elizaos agent start --path packages/agents/vibeeAgent.json
```

### Создание нового агента:

```bash
# 1. Создать через CLI
elizaos create --type agent

# 2. Переместить в packages/agents/ (если создан не там)
mv my-agent.json packages/agents/myAgent.json

# 3. Обновить этот README
```

## 📋 Список агентов

| Файл | Имя | Описание |
|------|-----|----------|
| `vibeeAgent.json` | VIBEE | Главный AI-агент проекта |
| `kolsAgent.json` | KOLs Agent | Мониторинг групп и KOLs |
| `neuroPhoto.json` | NeuroPhoto | Генерация изображений |
| `instagramExpert.json` | Instagram Expert | Автоматизация Instagram |

## 📚 Документация

Подробная документация: [`AGENTS_CENTRALIZED.md`](../../AGENTS_CENTRALIZED.md)

---

**Последнее обновление**: 2025-11-29

