# 📊 Codecov Integration Setup

## ✅ Что настроено

### Измененные файлы:
1. **`.github/workflows/ci.yaml`** - добавлен шаг загрузки coverage в Codecov
2. **`packages/core/bunfig.toml`** - настроен v8 coverage provider с LCOV репортером
3. **`packages/core/package.json`** - добавлен скрипт `test:coverage:lcov`

### Установленные пакеты:
- `@vitest/coverage-v8@4.0.13` - провайдер покрытия для vitest/bun
- `nyc@17.1.0` - инструмент для работы с coverage (резервный)

## 🚀 Как использовать

### Локальный запуск с покрытием:
```bash
# Запуск с LCOV (для Codecov)
cd packages/core
bun run test:coverage:lcov

# Результат: coverage/lcov.info (720KB)
```

### В GitHub Actions:
- Автоматически запускается при `push` в `main` или `pull_request`
- Генерирует LCOV отчет
- Загружает в Codecov с токеном

## 🔑 Настройка GitHub Secrets

Добавьте в GitHub Repository → Settings → Secrets and variables → Actions:

```
CODECOV_TOKEN=8e17156b-fa52-4097-b897-e4f61a2b019e
```

## 📈 Проверка работы

### После следующего коммита:
1. Перейдите на https://app.codecov.io/gh/vibee/vibee-eliza-999
2. Увидите отчет о покрытии кода
3. Бейджи появятся в README.md

### Локальная проверка:
```bash
cd packages/core
rm -rf coverage
bun run test:coverage:lcov
ls -lah coverage/lcov.info  # должен быть 700KB+
```

## 📊 Метрики покрытия

- **Всего тестов:** 705
- **Проходят:** 701 ✅
- **Пропущено:** 4
- **Не проходят:** 0 ❌
- **Покрытие:** ~50-70% (по функциям и строкам)

## 🛠️ Troubleshooting

### Ошибка: "No coverage files found"
**Решение:** Убедитесь, что добавили `--coverage-reporter=lcov` в команду

### Ошибка: "Codecov upload failed"
**Решение:** Проверьте, что `CODECOV_TOKEN` добавлен в GitHub Secrets

### Ошибка: "token not found"
**Решение:** Токен уже добавлен: `8e17156b-fa52-4097-b897-e4f61a2b019e`

## 📝 Дополнительная информация

- **Codecov Dashboard:** https://app.codecov.io/gh/vibee/vibee-eliza-999
- **Coverage Directory:** `packages/core/coverage/`
- **LCOV File:** `packages/core/coverage/lcov.info`
- **CI Workflow:** `.github/workflows/ci.yaml` (строки 47-53)

---

**Статус:** ✅ **ГОТОВО К ИСПОЛЬЗОВАНИЮ**

Дата настройки: 2025-11-24
Версия: 1.0.0
