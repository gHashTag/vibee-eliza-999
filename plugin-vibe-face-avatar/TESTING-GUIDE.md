# 🧪 Руководство по тестированию Chat функциональности

## 🚀 Быстрый старт

### 1. Запуск backend
```bash
cd /Users/playra/vibee-agent
npm run dev
```
**Порт:** 3001

### 2. Запуск frontend
```bash
cd /Users/playra/vibee-agent/plugin-vibe-face-avatar/src/frontend
npm run dev
```
**Порт:** 5175

### 3. Открыть чат
```
http://localhost:5175/chat
```

---

## 🎮 Ручное тестирование

### Полный цикл работы:

#### Шаг 1: Загрузка фото
- Перетащите 10-25 фотографий себя в поле ввода
- Или нажмите на иконку 📎 и выберите файлы
- **Ожидаемый результат:** Появится сообщение "Фото загружено! Прикреплено X файлов"

#### Шаг 2: Обучение модели
```
/face train Мой цифровой двойник
```
**Ожидаемый результат:**
- Сообщение "Модель 'Мой цифровой двойник' создана"
- Статус "отправлена на обучение"
- Модель появляется в списке

#### Шаг 3: Генерация изображения
```
/neurophoto я в стиле киберпанк
```
**Ожидаемый результат:**
- Сообщение "Изображение сгенерировано!"
- Отображается сгенерированное изображение
- Указывается использованная модель

---

## 🧪 Автоматическое тестирование (Cypress)

### Запуск с GUI (рекомендуется)
```bash
cd /Users/playra/vibee-agent/plugin-vibe-face-avatar/src/frontend
npm run cy:open
```

**Что увидите:**
- Откроется окно Cypress Test Runner
- Выберите браузер (Chrome/Electron)
- Нажмите на тест `chat-workflow.cy.ts`
- Нажмите "Start Testing"

### Запуск в CLI
```bash
npm run cy:run
```

**Что делает тест:**
1. ✅ Открывает страницу чата
2. ✅ Проверяет интерфейс
3. ✅ Загружает тестовый файл
4. ✅ Проверяет обучение модели
5. ✅ Проверяет генерацию изображения
6. ✅ Проверяет обработку ошибок

### Видеозапись тестов
Видео сохраняется в:
```
cypress/videos/chat-workflow.cy.ts/[timestamp].mp4
```

Скриншоты при ошибках:
```
cypress/screenshots/chat-workflow.cy.ts/[timestamp].png
```

---

## 📋 Тестовые сценарии

### ✅ Успешные сценарии

1. **Полный workflow**
   - Загрузка фото → `/face train` → `/neurophoto`
   - **Файл:** `chat-workflow.cy.ts` - `should complete full workflow`

2. **Загрузка файлов**
   - Перетаскивание в поле ввода
   - **Файл:** `chat-workflow.cy.ts` - `should update placeholder based on state`

3. **Обучение модели**
   - С названием: `/face train Модель`
   - **Файл:** `chat-workflow.cy.ts` - `should complete full workflow`

4. **Генерация изображения**
   - С промптом: `/neurophoto описание`
   - **Файл:** `chat-workflow.cy.ts` - `should complete full workflow`

### ❌ Негативные сценарии

5. **Обучение без фото**
   ```
   /face train Тестовая модель
   ```
   **Ожидаемое:** "Сначала загрузите 10-25 фотографий"
   - **Файл:** `chat-workflow.cy.ts` - `should show error when trying to train without photos`

6. **Без названия модели**
   ```
   /face train
   ```
   **Ожидаемое:** "Введите название для вашей модели"
   - **Файл:** `chat-workflow.cy.ts` - `should ask for model name when not provided`

7. **Генерация без промпта**
   ```
   /neurophoto
   ```
   **Ожидаемое:** "Укажите описание для генерации"
   - **Файл:** `chat-workflow.cy.ts` - `should show error when generating without prompt`

8. **Неизвестная команда**
   ```
   рандомный текст
   ```
   **Ожидаемое:** Список доступных команд
   - **Файл:** `chat-workflow.cy.ts` - `should show help message for unknown commands`

---

## 🎯 Проверяемые элементы

### UI элементы
- ✅ Заголовок "Добро пожаловать в чат VIBEE!"
- ✅ Инструкции с номерами 1, 2, 3
- ✅ Поле ввода (textarea)
- ✅ Кнопка отправки
- ✅ Индикатор загруженных файлов
- ✅ Список моделей

### Состояния
- ✅ Loading состояние
- ✅ Ошибки валидации
- ✅ Placeholder текст
- ✅ Pending файлы
- ✅ Автопрокрутка

### Интерактивность
- ✅ Загрузка файлов (drag & drop)
- ✅ Отправка команд
- ✅ Получение ответов
- ✅ Навигация между состояниями

---

## 🔧 Настройка тестов

### cypress.config.ts
```typescript
export default defineConfig({
  e2e: {
    baseUrl: "http://localhost:5175",  // URL frontend
    video: true,                       // Включить запись видео
    screenshotOnRunFailure: true,      // Скриншоты при ошибках
    defaultCommandTimeout: 10000,      // Таймаут команд
  },
});
```

### Поддержка файлов
- **cypress/support/e2e.ts** - Глобальная конфигурация
- **cypress/support/commands.ts** - Кастомные команды

### Фикстуры
- **cypress/fixtures/** - Тестовые данные
- **cypress/downloads/** - Загруженные файлы

---

## 📊 Отчет о покрытии

| Компонент | Тесты | Статус |
|-----------|-------|--------|
| Загрузка фото | ✅ | Пройдено |
| Обучение модели | ✅ | Пройдено |
| Генерация | ✅ | Пройдено |
| Ошибки | ✅ | Пройдено |
| UI состояния | ✅ | Пройдено |
| API интеграция | ✅ | Пройдено |

---

## 🚨 Диагностика проблем

### Если тесты падают:

1. **Проверьте backend**
   ```bash
   curl http://localhost:3001/api/health
   ```

2. **Проверьте frontend**
   ```bash
   curl http://localhost:5175/chat
   ```

3. **Посмотрите логи Cypress**
   - Console в окне Cypress
   - Скриншоты в `cypress/screenshots/`
   - Видео в `cypress/videos/`

4. **Проверьте network**
   - DevTools → Network tab
   - Должны быть запросы к API

---

## 🎬 Демонстрация

### Просмотр видео тестов:
```bash
# Найти последнее видео
ls -lt cypress/videos/ | head -5

# Открыть видео
open cypress/videos/chat-workflow.cy.ts/[timestamp].mp4
```

### Скриншоты ошибок:
```bash
open cypress/screenshots/chat-workflow.cy.ts/[timestamp].png
```

---

## ✨ Результат

После запуска тестов вы увидите:

1. **Автоматизированные тесты** проходят все сценарии
2. **Видеозапись** показывает каждый шаг
3. **Скриншоты** сохраняются при ошибках
4. **Отчет** в консоли показывает результаты

**Все тесты должны пройти ✅**

---

## 📝 Заключение

- ✅ **Функциональность полностью протестирована**
- ✅ **E2E тесты покрывают все сценарии**
- ✅ **Видеозапись показывает работу**
- ✅ **Готово к продакшену**

**Система полностью готова к использованию! 🚀**
